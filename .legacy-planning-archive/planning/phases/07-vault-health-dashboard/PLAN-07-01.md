---
phase: 07
plan: 01
type: feature
autonomous: true
complexity: MODERATE
---

# Vault Health Dashboard

## Objective

Build a `/vault` page that displays operational health metrics for MuninnDB,
including engram counts, storage usage, coherence scores, and engram type
breakdown. Follows all established patterns from Phases 04-06.

## Context

@packages/luca-observer/app/learning/page.tsx
@packages/luca-observer/hooks/use-learning-evolution.ts
@packages/luca-observer/components/learning/category-breakdown.tsx
@packages/luca-observer/components/learning/learning-stats.tsx
@packages/luca-observer/lib/muninn-types.ts
@packages/luca-observer/lib/constants.ts
@packages/luca-observer/components/layout/sidebar.tsx

## Tasks

### Task 1: Add navigation item and format utility

type="auto" testable="false"

- Add "Vault" nav item to NAV_ITEMS in `lib/constants.ts` with `Database` icon
- Add `Database` icon import and ICON_MAP entry in `components/layout/sidebar.tsx`
- Add `formatBytes` utility to `lib/format.ts` for human-readable storage display

**Verification:** `bunx --bun tsc --noEmit` passes from packages/luca-observer

### Task 2: Create useVaultHealth hook

type="auto" testable="false"

- Create `hooks/use-vault-health.ts` following the use-learning-evolution pattern
- Fetch from `/api/muninn/stats` and `/api/muninn/engrams?limit=500`
- Compute: vault overview stats, coherence data, engram type breakdown, storage info
- Export typed interfaces for all derived data

**Verification:** `bunx --bun tsc --noEmit` passes from packages/luca-observer

### Task 3: Create VaultOverview and StorageInfo components

type="auto" testable="false"

- Create `components/vault/vault-overview.tsx` — stats cards grid (engram count, vault count, index size, storage)
- Create `components/vault/storage-info.tsx` — storage metrics card with formatted bytes

**Verification:** `bunx --bun tsc --noEmit` passes from packages/luca-observer

### Task 4: Create CoherenceMetrics and EngramTypeBreakdown components

type="auto" testable="false"

- Create `components/vault/coherence-metrics.tsx` — coherence score display per vault
- Create `components/vault/engram-type-breakdown.tsx` — horizontal bar chart (reuse Phase 06 pattern)

**Verification:** `bunx --bun tsc --noEmit` passes from packages/luca-observer

### Task 5: Create vault page and verify

type="auto" testable="false"

- Create `app/vault/page.tsx` following the learning page pattern
- PageContainer + title + subtitle + actions (refresh + last updated)
- Loading skeletons, ErrorBoundary wrappers for each section

**Verification:** `bunx --bun tsc --noEmit` passes from packages/luca-observer

## Verification

- [ ] `bunx --bun tsc --noEmit` passes with zero errors from packages/luca-observer
- [ ] Navigation shows "Vault" item with Database icon
- [ ] /vault page renders with all 4 component sections
- [ ] Storage bytes are formatted as human-readable (KB, MB, GB)

## Success Criteria

- Vault Health Dashboard page accessible at /vault
- Hook fetches stats + engrams and computes derived metrics
- All components follow established card/chart patterns
- Type-safe with zero TypeScript errors
