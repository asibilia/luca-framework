# Milestone v13.0.0-pai-learnings — Audit Summary

**Status:** PASSED · **Closed:** 2026-06-15

## Accomplishments (REQ-01 … REQ-10)

1. **Plan quality** (REQ-01/02): first-class `--area`/`--priority` on `luca todo` + unknown-flag rejection; plan criteria grammar (Splitting Test, mandatory anti-criteria, ID-stability + tombstones) with `luca plan lint`.
2. **Verification doctrine** (REQ-03): evidence-per-criterion probe table, forbidden-language list, `[DEFERRED-VERIFY]` (schema `superRefine`: deferred ⇒ met:false + tracked follow-up), ReReadCheck, deliverable manifest; verify-handler routed through `writeVerificationResult` (runId-stamped).
3. **Signal capture & telemetry** (REQ-04/05): `signal.satisfaction` (3-path; implicit outcome PRIMARY so full-auto runs are never signal-empty), `signal.failure-dump`, `classifier.override` — all on the open `TelemetryKind` union via `luca telemetry emit` (no new CLI verb); learner signal synthesis + session-resume readback.
4. **Learning loop** (REQ-06/07): Deutsch C/R/L learner format (conjectured/refuted_by/learned/criterion_now, carried in TO_PERSIST content — zero contract change); mandatory **Gotchas** field on all 20 agent/subagent artifacts (optional Zod + parity audit, `renderGotchasPrelude`).
5. **Governance** (REQ-08/09): hard-vs-soft floors audit (`docs/decisions/governance-floors-audit.md`) + closed `RELAXATION_PATHS` enumeration; trimmed over-prescriptive instruction text across 7 bodies (load-bearing constraints preserved); fixed phantom `luca todo move`/`move-batch`/`retro postmortem gate` verbs.
6. **Cross-vendor** (REQ-10): independence reviewer perspective + gated CRITICAL-only opt-in audit step — honestly framed (single-vendor harness → independence approximates cross-vendor).

## Quality

- Every phase: **verify PASS + review APPROVE**. `bunx --bun tsc --noEmit` green throughout. Postmortem gate clean (`luca retro` exit 0).
- Phases 3–6 each took a review-driven fix cycle; phase 7 converged first-pass.

## Dominant cross-cutting learning

The **phantom-capability / field-drift / meta-doc-drift** class recurred in 6/7 phases (phantom CLI verbs, runId-vs-sessionId field drift, gotchas misstating mechanics, an incomplete "closed" enumeration). Verifier presence/token probes systematically MISS it — only code review caught it. **Recommended follow-up:** a standing instruction-body ↔ CLI/schema-registry lint to move the catch from per-phase review to a deterministic gate.

## Follow-up todos (MuninnDB backlog)
- Rebuild stale `.claude/hooks` bundles (pipeline-guard transition-table drift).
- Consolidate `sanitizeControlChars` (claim-verify local copy → shared helper).
- Fix phantom `luca todo move`/`move-batch` in finalize/execute mode **bodies** (gotchas fixed; bodies pending).
- Standing instruction-body phantom-verb lint.

## Notes
- Mid-session state recovery occurred (a concurrent milestone clobbered `.luca/state.json`); recovered via roadmap re-seed + `set-current-phase` + `claim-owner`.
- Git tag + phase-archive intentionally deferred: this milestone ships as feature branch / PR #300 (unmerged); tag-on-release and archive happen at merge / next `/milestone-new`.
