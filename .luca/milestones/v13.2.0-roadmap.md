# v13.2.0 — Deterministic Agentic Development (DAD) — Milestone Roadmap

> Milestone snapshot for the DAD / XState statechart migration. Branch `dad-xstate-migration`. Spec anchor `d35ea81`. Named v13.2.0 to avoid a filename collision with the pre-existing (mislabeled) `v13.1.0-roadmap.md`.

## Goal

Replace Luca's seven hand-rolled pipeline `Record` tables — the de-facto pipeline state machine — with **one XState v5 statechart**, used statelessly: a single visualizable definition of pipeline control flow. Make the schema-declared-but-never-incremented fix-loop iteration budget **live**.

## Phases (all complete)

| # | Trace ID | Deliverable | Status |
|---|----------|-------------|--------|
| 1 | DAD-P0 | Hygiene: excise dead `src/iteration/*` toolkit, repair `iterationPlan` prose, reconcile the `architect` double-def | ✅ complete |
| 2 | DAD-P1a | **THE GATE** — XState machine + `machineVerdict` adapter + 169-pair golden parity harness (path B, `xstate@5.32.2`) | ✅ complete |
| 3 | DAD-P1b | Repoint `luca state advance` to the stateless machine (`decideAdvance` → `machineVerdict`); hook stays cold | ✅ complete |
| 4 | DAD-P1c | Fix-loop budget guard live as `assign` actions + `fixloop.counted` telemetry (advisory-first) | ✅ complete |
| 5 | DAD-P1t | Delete `PIPELINE_STEP_TO_COARSE_PHASE` → derive from machine `getMeta()`; demote 4 tables to data | ✅ complete |
| 6 | DAD-P1d | `luca graph` verb → Mermaid `stateDiagram-v2` + JSON | ✅ complete |
| 7 | DAD-P2 | Persistent-runner POC (`luca start`) — go/no-go = **GO**, 5 acceptance tests pass | ✅ complete |

## Dependency gate (honored)

`DAD-P0` (anytime) → `DAD-P1a` (THE GATE — green before P1b/P1c/P1t) → { P1b → {P1c, P1t}, P1d } → (all of Phase 1) → `DAD-P2` (go/no-go, not a commitment).

## Outcome

Seven hand-rolled tables → one visualizable statechart, the sole control-flow authority. The previously-inert iteration budget is now live (advisory-first). Net +2309/−453 across 31 files, 30 commits. Backward-compatible: no schema break, cold-process enforcement preserved.
