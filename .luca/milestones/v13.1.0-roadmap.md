# Milestone v13.1.0 — Telemetry Impact & Attribution (Roadmap Snapshot)

Closed: 2026-06-16 · PR #308 (closes #307) · branch `307--telemetry-impact-attribution`

**Theme:** Close the gap between activity-capture (which Luca already did well) and impact-measurement. Build on the existing capture layer (TelemetryRecord v1, signal.satisfaction/failure-dump, partial record-recall) — turn captured telemetry into impact/attribution answers. No schema `v` bump anywhere.

| # | Phase | REQ | Complexity | Status |
|---|-------|-----|-----------|--------|
| 1 | recall-outcome-attribution | REQ-11, REQ-12 | COMPLEX | ✅ complete |
| 2 | cost-per-outcome-report | REQ-13 | MODERATE | ✅ complete |
| 3 | pr-outcome-writeback | REQ-15 | MODERATE | ✅ complete |
| 4 | outcome-kpi-persistence (capstone) | REQ-14 | MODERATE | ✅ complete |

## Phase detail

**1 · recall-outcome-attribution (REQ-11/12).** Wire `record-recall` telemetry at all 5 modes (triage/architect/execute/review/finalize) for per-mode hit-rate + verified-tier rate; add a `recall.utilization` memory-utilization signal correlating recalled concept IDs to outcome valence.

**2 · cost-per-outcome-report (REQ-13).** `luca-telemetry-report` extended with a model rate table, token→cost conversion, cost-per-outcome (cost / phases-completed and / first-pass-success), and structure-vs-executor token attribution.

**3 · pr-outcome-writeback (REQ-15).** Two new telemetry kinds — `pr.created` (create-time, originating run) and `pr.outcome` (post-merge, fixed `pr-outcomes` synthetic log) — joined on `meta.prNumber`; a `luca telemetry pr-outcome` write verb (merged/reverted, review rounds, time-to-merge); a finalize-side `pr.created` run→PR map directive; report-side merge-rate / time-to-merge aggregation.

**4 · outcome-kpi-persistence (REQ-14, capstone).** A pure `computeOutcomeKpis` core fn behind a read-only `luca telemetry kpi --json` verb computes complexity-bucketed KPIs (low-confidence ratio, first-pass verify rate, mean rework iterations, re-entry rate) from per-phase `confidence.jsonl`/`verify.json` + `signal.satisfaction` telemetry; a `finalize` body directive persists them at milestone close as milestone-stamped `metric:outcome-kpi-<version>-<complexity>` memories to the config-resolved repo vault. Producer-side, `--slug`/`--complexity` stamped onto the orchestrator's `signal.satisfaction` emits (forward-only) so run telemetry becomes complexity-bucketable.

> Scope note: REQ-14 originally planned MVP-defer of 2 KPIs (no clean per-phase source); a confidence-gate redirect brought all 4 in-scope via producer-side telemetry instrumentation (FULL scope shipped).
