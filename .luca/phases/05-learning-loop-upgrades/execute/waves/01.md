# Round 1 — plumbing + learner C/R/L body

## Task A.1 (define/agent.ts + subagent.ts) — executor attestation
- ac-01: grep gotchas agent.ts=1, subagent.ts=1 (0 as-built). ac-02: `gotchas: z.array(z.string()).default([])` at agent.ts:134, subagent.ts:192 — optional, NOT .min(1).
- anti-05: no `.min(1)` on gotchas. tsc exit 0.

## Task A.2 (compile/render-body.ts) — executor attestation
- ac-03: `renderGotchasPrelude` present. ac-04: grep gotchas = 7 (≥3: BodyRenderInput field + renderBody `input.gotchas ?? []` wiring + prelude param/body/doc).
- anti-10: 0 Date.now/Math.random (idempotent, mirrors renderGuidancePrelude, empty string for empty array). BodyRenderInput.gotchas optional-with-default so emitter sites don't break standalone. tsc exit 0.

## Track B (subagents/learner.ts body) — executor attestation
- ac-09: grep -ci `conjectur|refuted_by|criterion_now` = 9 (0 as-built). Extraction shape restructured in-place to CONJECTURED/REFUTED_BY/LEARNED/CRITERION_NOW (kept LEARNING_TYPE/CONCEPT/CONFIDENCE for routing); learn.md write section renders C/R/L; Signal Synthesis unchanged.
- ac-11: TO_PERSIST `content:` carries C/R/L, vault/concept/content/tags keys unchanged. anti-06: no Zod C/R/L schema. anti-07: `### TO_PERSIST` = 1 (single block). Body only — no top-level gotchas field added (deferred to A.4b). tsc exit 0.
