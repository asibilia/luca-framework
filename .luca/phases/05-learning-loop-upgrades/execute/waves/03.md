# Round 3 — review fix cycle (gotcha-content corrections)

Both reviewers REQUEST_CHANGES — the irony: the authored gotchas (REQ-07's accurate-footgun feature) themselves contained the phantom-capability/field-drift class.

## MUST-FIX 1 — finalize.ts gotcha named nonexistent `luca todo move-batch`/`move`
`luca todo` exposes only add|list|update (write-surface/todo.ts:30/108/152); move/move-batch are legacy mastracode. finalize is a mode-agent WITH Bash → would hit "unknown command" at milestone close. Fixed: re-pointed gotcha to `luca todo update --id <id> --status done --verification-criterion <ac-id>` (real verb, --verification-criterion at todo.ts:208); kept milestone-close intent.

## MUST-FIX 2 — researcher.ts gotcha misstated the stage-gate
Said "research.md is illegal at the plan step" — wrong twice: researcher runs at the `research` step, and research.md IS legal there (step-artifacts.ts:43 research:['research']). Real reason: researcher has no Write tool (allowedTools Read/Grep/Glob), returns in-context; parent research MODE owns the research.md write. Fixed: re-anchored to "read-only, no Write tool — parent research mode persists research.md."

## SHOULD-FIX 3 — learner.ts gotcha named `luca retro postmortem`
Real command is bare `luca retro` (retro.ts:31, no postmortem subcommand). Fixed gotcha :36 + body :44 → `luca retro`.

## Pre-existing bug surfaced (filed as todo, OUT of phase-05 scope)
execute.ts:411 + finalize.ts:470/474/476 BODIES (not gotchas) instruct `luca todo move`/`move-batch` — same phantom verbs. Real path: `luca todo update --status done`. Would error at milestone close. Todo `phantom-cli-verbs-finalize-execute-todo-move-move-batch` filed (overlaps phase-06 governance-audits instruction-text audit).

## Evidence
researcher `illegal at the plan`=0; learner gotcha/body `retro postmortem`=0; finalize gotcha move-batch only as negation/warning; `gotchas:`≥1 in all 3; learner `### TO_PERSIST`=1; tsc exit 0; `luca checks run` passed.
