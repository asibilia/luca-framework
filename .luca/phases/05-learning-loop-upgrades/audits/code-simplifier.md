PERSPECTIVE: simplification + dx
VERDICT: REQUEST_CHANGES

FINDINGS:

- [MUST-FIX] Researcher subagent gotcha misstates a real Luca mechanic. The first gotcha reads "research.md is illegal at the plan step". This is wrong on two counts: (1) the researcher runs at the `research` step, not `plan`; (2) `research.md` is the LEGAL artifact at the `research` step — `STEP_ARTIFACTS` maps `research: ['research']` (packages/luca-core/src/state/configs/step-artifacts.ts:43). The real reason the researcher subagent must not write a file is purely role-based: it has no `Write` tool (allowedTools is Read/Grep/Glob) and returns findings in-context; the research MODE owns the synthesis write to research.md (modes/research.ts:42,51,107). A subagent reasoning about the stage-gate would be misled into believing research.md is contract-illegal when it is not. Per the audit charter a gotcha that misstates a Luca mechanic is a MUST-FIX (it would mislead every researcher spawn).
  File: packages/luca-tools/src/artifacts/subagents/researcher.ts:35
  Suggestion: Re-anchor to the actual reason. e.g. "You are read-only (no Write tool) — you do NOT write research.md or any `.luca/` artifact; return your dimension findings in-context for the research mode to persist into the synthesized research.md. The canonical write is the parent mode's, not yours." Drop the "illegal at the plan step" clause entirely.
  Cross-phase: false

- [SHOULD-FIX] Mode/subagent gotcha vs body redundancy is acceptable but the research-mode `raw/research-<NN>.md` claim rides on an unverified contract assumption. The research-mode body (modes/research.ts:107-111, pre-existing, NOT part of the phase-05 delta) asserts `raw/research-<NN>.md` is "in the LUCA_DIR_CONTRACT `raw/` slot per the validator". The planning-structure contract I can see (phase-dir allowlist) does NOT list a `raw/` slot — the allowed phase files are research.md/context.md/plan.md/plan-review.md/verify.json/learn.md/execute/*/audits/*. The phase-05 gotcha at modes/research.ts:308 faithfully restates the body, so it is internally consistent, but if `raw/` is not actually in the shipped contract the gotcha (and body) point agents at a path the stage-gate would reject. This is out of the cold-isolation delta scope (body unchanged) but flagging because the gotcha amplifies it.
  File: packages/luca-tools/src/artifacts/modes/research.ts:308
  Suggestion: Out of phase-05 scope to fix, but verify `raw/` is an actual LUCA_DIR_CONTRACT slot before relying on it; if it is not, both body and gotcha need correction in a follow-up.
  Cross-phase: true

- [NOTE] renderGotchasPrelude / renderGuidancePrelude duplication is justified — do NOT extract. The two functions share only a trivial 1-line skeleton (`['## Header', '', ...items].join('\n')`) plus an empty-guard. Their item-construction differs fundamentally: renderGuidancePrelude maps four boolean flags to fixed canned prose; renderGotchasPrelude maps author-supplied strings 1:1 to bullets. The same skeleton also appears in renderPipelineInvocationPrelude and renderTelemetryPrelude, each with its own item shape. A shared `renderBulletBlock(header, items)` helper would deduplicate ~2 lines across 4 callers while adding an indirection that obscures the per-block intent — this is the load-bearing-duplication case (cf. phase-03). The mirror is correct as written. The doc comment at render-body.ts:138-146 explicitly justifies the parallel (parity-audit fingerprinting), which is the right rationale.
  File: packages/luca-tools/src/compile/render-body.ts:147

- [NOTE] No over-engineering in the schema/emitter wiring. `gotchas: z.array(z.string()).default([])` (optional, not `.min(1)`) is the minimal change that satisfies REQ-07 without a module-load flag-day (Q1 decision honored, anti-05). The emitters pass `gotchas: def.gotchas` through with no transformation (emit-agent.ts:60, emit-subagent.ts:69); BodyRenderInput's `gotchas?` is optional with a `?? []` at the call site (render-body.ts:76) — defensively redundant with the schema default but harmless and self-documenting. No notes-worthy complexity.

- [NOTE] Gotcha CONTENT quality is high across the spot-checked 10. Triage (orchestrator-held complexity / research-only legal next step / ≤75-word cap), plan (stock vs architect distinction, no submit_plan tool), execute (bash-commit DENIED in EXECUTING — verified against classify-bash-command.ts:87,415; never `git add .`; convergence hard-stop at iteration>=3), review (5 MUST-FIX cap, deferred:true never MET, DROPPED-tombstone verificationRef rejection), finalize (finalize→finalize illegal self-transition, move-batch index reshuffle), executor (git add explicit files only), verifier (verify.json writable only at verify step, KEEP anti-criteria), reviewer (≥3 file:line for APPROVE), and learner (no MCP/Bash, signal-digest injection, content-carry key invariant) are all REAL, specific, stage-accurate footguns that restate a verifiable mechanic — not filler. Mechanic claims I probed (bash-commit classification, STEP_ARTIFACTS, finalize self-transition) check out. Only the researcher gotcha (above) is wrong.

- [NOTE] Deutsch C/R/L learner shape reads coherently to an LLM. The extraction block (learner.ts:62-70) labels CONJECTURED/REFUTED_BY/LEARNED/CRITERION_NOW with inline guidance per field; CRITERION_NOW is unambiguous — "the new test/check/guard that catches a recurrence of this error" clearly asks for a testable check (reinforced at :85 "the check that catches a recurrence" and the TO_PERSIST content template at :106 "Criterion now: <the check that catches a recurrence>"). The C/R/L is NOT bolted awkwardly onto the old shape: LEARNING_TYPE/CONCEPT/CONFIDENCE are explicitly retained for routing/dedup (:72) and the narrative is the learning body. The content-carry invariant (keys stay vault/concept/content/tags; C/R/L rides INSIDE content as prose) is stated three times (:38 gotcha, :106 template, :110 explicit) so an LLM cannot miss it — zero orchestrator-contract drift, honoring Q2/anti-07. learn.md rendering (:85) renders the four labelled lines coherently.

CONSOLIDATED:
  MUST_FIX_COUNT: 1
  SHOULD_FIX_COUNT: 1
  NOTE_COUNT: 4
  CROSS_PHASE_COUNT: 1
