# Wave 4 — review fixes (cycle 2), fully parallel ×3 grouped executors

| Task | Status | Criteria | Files |
|---|---|---|---|
| 1.4.1 classifier registration (MUST-FIX) | ✓ | ac-14 ✓ — probe before: `luca plan lint --file x.md` → bash-mutate; after: bash-readonly (allowed in every STAGE_TOOL_MATRIX row) | hook/helpers/classify-bash-command.ts (LUCA_NOUN_VERBS:250, LUCA_READ_VERBS:226) |
| 1.4.2 WRITE_COMMAND_PHASES entry | ✓ | ac-15 ✓ (`'plan lint': []` at step-artifacts.ts:87). []-semantics VERIFIED safe: runWriteHandler:53-66 skips phase check when length 0 → "allowed anywhere", not "nowhere"; sole consumer | luca-core/src/state/configs/step-artifacts.ts (legal — anti-01 guards verification/ only) |
| 1.4.3 lint robustness | ✓ | ac-16 ✓ (backtick-masked ` and ` → 0 warnings), ac-17 ✓ (` AND ` flagged, /i), ac-18 ✓ (ESC sanitized in warning+summary+error branches incl. err.message), ac-19 ✓ ("exits 0 on lint findings"; zero "always exits 0" remnants) | handlers/luca-plan-lint.ts (maskInlineCodeSpans — same-length space masking keeps columns; sanitizeControlChars \xNN), commands/write-surface/plan.ts (header comment re-attributed to explicit [] entry) |
| 1.4.4 split-parent fate + ## Decisions | ✓ | ac-20 ✓ (rule :246 + template :217), ac-21 ✓ (verifier :87), ac-22 ✓ (round-trip fixture: SPLIT parent + .1/.2 + anti → 0 warnings, exit 0, run LAST post-1.4.3), ac-26 ✓ (## Decisions 0→1), ac-27 ✓ (review.ts :82 non-tombstoned, non-split-parent) | modes/architect.ts, subagents/verifier.ts, modes/review.ts |
| 1.4.5 phase-plan mirror Rule 3 + lint mention | ✓ | ac-23 ✓ (:34 tombstones), ac-24 ✓ (:49 plan lint invocation) | commands/phase-plan.ts |
| 1.4.6 skill brief drift restore | ✓ | ac-25 ✓ (:363 "substitute for review") | skills/phase-plan/index.ts |

Post-wave harness: passed (tsc exit 0).

Deviations (logged to confidence.jsonl):
- 1.4.3: err.message sanitized wholesale (ENOENT embeds raw path — required for ac-18 error branch); handler descriptor "Always succeeds" tightened too (same inaccuracy as plan.ts).
- 1.4.4: `## Decisions` placed after Risks & Mitigations in template (position unspecified; purely additive).
- Probe mechanics: runMain is zero-arg (citty, cli.ts:94) — probes set process.argv before import.
- ops: .luca/tmp/checks.json key is `name` (not `label`) per current schema — file updated externally mid-phase; harness green with it.
