PERSPECTIVE: architecture
VERDICT: REQUEST_CHANGES
FINDINGS:
- [MUST-FIX] Instruction bodies invoke claim-verify subcommand grammars that do not exist on the shipped CLI. The phase-03 CLI surface is `luca claim-verify <file>` with only a positional `file` and `--repo-root` (packages/luca-cli/src/commands/claim-verify.ts:20-44; registered as a single command in packages/luca-cli/src/cli.ts:25-27 — no subcommands). But the agent prompts instruct: `luca claim-verify verify-text --text "<full review output>"` (packages/luca-tools/src/artifacts/modes/review.ts:205), `luca claim-verify verify-file --path <planFile>` (packages/luca-tools/src/artifacts/modes/finalize.ts:200), and `luca claim-verify gate --paths ... --texts ...` (finalize.ts:310, restated at finalize.ts:467 and :471). An agent following finalize Step 3c/5b.2 will pass `verify-file`/`gate` as the positional file, get an `artifact-unreadable` failure, exit 1, and spuriously block (or thrash) the finalize gate; `verify-text --text` has no text-input mode at all. There is also no `code: CLAIM_VERIFICATION_FAILED` envelope (finalize.ts:313) — the CLI emits logger lines + exit code.
  File: packages/luca-tools/src/artifacts/modes/finalize.ts:200; packages/luca-tools/src/artifacts/modes/finalize.ts:310; packages/luca-tools/src/artifacts/modes/review.ts:205; packages/luca-cli/src/commands/claim-verify.ts:20
  Suggestion: Pick one grammar. Either (a) extend the citty command with `verify-file`/`verify-text`/`gate` subcommands (text input can stage through `.luca/tmp/<name>.md` per the tmp contract), or (b) rewrite the three instruction-body call sites to the shipped `luca claim-verify <file>` form and drop the `CLAIM_VERIFICATION_FAILED` envelope reference in favor of exit-code semantics.
  Cross-phase: true
- [MUST-FIX] The verifier subagent's canonical verify.json template omits the schema-REQUIRED `timestamp` field. VerificationResultSchema requires `timestamp: z.string()` (packages/luca-core/src/verification/schemas.ts:78-79), but the write template the verifier is told to copy (packages/luca-tools/src/artifacts/subagents/verifier.ts:125-138) lists wave/mode/status/criteria/checks/convergence/errorFingerprints/recommendation/notes/deliverables and never mentions `timestamp` (grep for `timestamp` in verifier.ts: 0 hits). The verifier writes via the native Write tool (verifier.ts:85), bypassing the safeParse in luca-phase-write-verify — so a template-faithful verify.json fails `VerificationResultSchema.safeParse` in `readVerificationResult` (packages/luca-core/src/verification/verification-result.ts:60-63) and is silently treated as ABSENT by review Step 1.4 (`luca verification read`) and finalize's `luca verification aggregate`, defeating the exact gates this phase hardens.
  File: packages/luca-tools/src/artifacts/subagents/verifier.ts:126
  Suggestion: Add `timestamp: "<ISO 8601>"` (and optionally `phase`) to the template block at verifier.ts:126-138 so the example is schema-complete.
  Cross-phase: false
- [SHOULD-FIX] Deferred invariants are documented but enforced nowhere on the write path. schemas.ts:27-35 states deferredFollowUp "is REQUIRED" when `deferred` is set and `met` "MUST be false", yet VerificationCriterionSchema has no `superRefine`, and the safeParse in luca-phase-write-verify (packages/luca-cli/src/write-surface/handlers/luca-phase-write-verify.ts:53) therefore accepts `deferred: true, met: true` with no follow-up id. Downstream consumers defend individually (aggregateVerificationResults treats deferred as blocking regardless of met — verification-result.ts:149; validate-verification-ref rejects deferred regardless of met — validate-verification-ref.ts:95), but the single cheapest enforcement point is the schema itself.
  File: packages/luca-core/src/verification/schemas.ts:33
  Suggestion: Add a `.superRefine` on VerificationCriterionSchema: `deferred === true` ⇒ `met === false` AND `deferredFollowUp` non-empty. Additive validation; legacy records without `deferred` are unaffected.
  Cross-phase: false
- [SHOULD-FIX] luca_phase_write_verify bypasses luca-core's `writeVerificationResult`, so the runId stamp is never applied on this write path. The luca-core writer's documented contract is to stamp `runId` so "a stale snapshot from a prior run cannot silently satisfy the new run's wave/phase guards" (verification-result.ts:86-99), but the handler serializes `parsed.data` itself via `writeAtomicFile` (luca-phase-write-verify.ts:73-75) — results written through the MCP surface are permanently "legacy" (no runId) and immune to the stale-run guard in readVerificationResult.
  File: packages/luca-cli/src/write-surface/handlers/luca-phase-write-verify.ts:73
  Suggestion: Route the handler through `writeVerificationResult({ cwd, slug, result: parsed.data, runId: <ctx run id if available> })` so stamping and serialization stay single-sourced in luca-core.
  Cross-phase: false
- [SHOULD-FIX] Deliverable-id format drift between producer and consumers. The plan grammar mints `**D1**` / `D<N>` ids (architect.ts:215-219; plan-lint DELIVERABLE_GRAMMAR `\*\*D\d+\*\*` at luca-plan-lint.ts:44-45), but the verifier example tells the verifier to write `id: "d-01"` (verifier.ts:116) and the schema doc echoes `"d-01"` (schemas.ts:54). "deliverable id from the plan" and the `d-01` example contradict each other, leaving the finalize ReReadCheck cross-reference (finalize.ts:161) without a deterministic join key.
  File: packages/luca-tools/src/artifacts/subagents/verifier.ts:116
  Suggestion: Declare one canonical form — simplest: verify.json `deliverables[].id` carries the plan D-id verbatim (`"D1"`); update the verifier example and the schemas.ts doc example to match.
  Cross-phase: false
- [NOTE] Lint check (g) deliberately accepts tombstoned and `[SPLIT → …]` parent ids as deliverable mapping targets (documented at luca-plan-lint.ts:166-171), while plan-reviewer item 10 declares split-parent pointers NOT valid mapping targets (plan-reviewer.ts:74). The existence-vs-judgment split is intentional and documented, but it is a standing drift risk between the two checkers — worth a cross-link comment in plan-reviewer.ts.
- [NOTE] `maskInlineCodeSpans` now exists in two packages (claim-verifier.ts:364, luca-plan-lint.ts:59) and `sanitizeControlChars` twice within luca-cli (luca-plan-lint.ts:76, luca-phase-write-verify.ts:21). The luca-core copy is boundary-justified (core must not import from cli, and the comment says so); the two intra-luca-cli copies could share a `write-surface/helpers/` module, matching the repo's cross-linked-copies convention.
- [NOTE] Stale comment: verification-doctrine.ts:3 still says "(wave 2 wires the interpolation)" — the interpolation IS wired (verifier.ts:81, executor.ts:83). Drop the parenthetical.

Checks performed that PASSED (evidence):
- Import direction: luca-tools → luca-core only. verification-doctrine.ts:9 imports `@alecsibilia/luca-core/claim-verifier`; subpath export exists (luca-core package.json:28); luca-tools depends on luca-core workspace:* (luca-tools package.json:6). No reverse import; luca-core's claim-verifier reimplements maskInlineCodeSpans locally precisely to avoid importing luca-cli (claim-verifier.ts:358-369).
- Schema back-compat: all phase-03 fields are additive optionals — `deferred`/`deferredFollowUp`/`probeType` (schemas.ts:33-48), `deliverables` (schemas.ts:108); pre-existing verify.json records still parse.
- Single source of truth: FORBIDDEN_LANGUAGE_PHRASES owned by luca-core (claim-verifier.ts:324-330) and interpolated into VERIFICATION_DOCTRINE (verification-doctrine.ts:30-32); barrel re-export present (claim-verifier/index.ts:7). Doctrine probe table ↔ probeType enum: 8 rows ↔ 8 enum values, aligned (doctrine table lines 17-26 ↔ schemas.ts:38-47).
- Digest discipline: execute.ts:262-267 and review.ts:80 carry explicit "canonical: VERIFICATION_DOCTRINE in artifacts/shared/verification-doctrine.ts" pointers rather than re-stating the full doctrine; full interpolation only in verifier/executor subagent bodies.
- Deferred semantics coherent across consumers: aggregate forces deferred → blocking gap + allCriteriaMet=false even on malformed met:true (verification-result.ts:147-173); todo→done gate rejects CRITERION_DEFERRED before the met check (validate-verification-ref.ts:93-104); review mode surfaces deferred as PARTIAL/UNMET never MET (review.ts:82, :171); finalize treats deferred as blocking gaps (finalize.ts:149). Advisory-only forbidden-language scan never feeds verdicts (claim-verifier.ts:77-80 doc, claim-verify.ts:67-73 exit-code isolation).
- Doctrine `[DEFERRED-VERIFY]` step 2's `--source deferred-verify:<slug>:<ac-id>` fits the todo schema (`source: z.string().max(120).optional()` — todos/schemas.ts:71).

CONSOLIDATED:
  MUST_FIX_COUNT: 2
  SHOULD_FIX_COUNT: 3
  NOTE_COUNT: 3
  CROSS_PHASE_COUNT: 1
