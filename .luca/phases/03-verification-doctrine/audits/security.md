PERSPECTIVE: security
VERDICT: REQUEST_CHANGES
FINDINGS:
- [MUST-FIX] Unsanitized echo of attacker-controllable strings in validateVerificationRef error messages — violates the phase-02 sanitizeControlChars precedent that this same delta correctly applies in luca-plan-lint.ts:76 and luca-phase-write-verify.ts:21. Echoed without sanitization: `opts.ref.criterionId` (z.string().min(1), unconstrained charset — todos/schemas.ts:52) at lines 85, 98, 108, 114; the joined raw verify.json criterionId list at lines 85-90; `found.deferredFollowUp` (raw verify.json string) at line 100; `result.status` (raw, file is cast not schema-parsed) at line 120; JSON.parse `err.message` at line 66. verify.json content is agent-authorable via native Write during the verify step (stage-gate permits the canonical path), so every one of these is a control-char / ANSI-escape injection path into tool output surfaced verbatim by luca-todo-update.ts:100 (`luca_todo_update: ${err.code} — ${err.message}`).
  File: packages/luca-cli/src/write-surface/helpers/validate-verification-ref.ts:66,85,98,108,114,120
  Suggestion: Add the same sanitizeControlChars helper (or export the existing one to a shared helper) and wrap every interpolated runtime string in the ValidationError messages — criterionId, the existing-ids join, deferredFollowUp, status, and the JSON.parse error message.
  Cross-phase: false
- [SHOULD-FIX] VerificationCriterionSchema documents but does not enforce its deferred invariants. JSDoc (schemas.ts:27-31) states that when `deferred: true`, `deferredFollowUp` is REQUIRED and `met` MUST be false — but there is no superRefine, so a crafted `{ deferred: true, met: true }` or `{ deferred: true }` (no follow-up) payload passes luca_phase_write_verify schema validation (luca-phase-write-verify.ts:53) and is persisted. The two consumers in scope defend correctly (validate-verification-ref.ts:95 checks deferred before met; verification-result.ts:149,171 treats deferred as blocking regardless of met), so there is no live gate bypass today — but findCriterion (verification-result.ts:110) returns raw criteria, and any future consumer keying on `met` alone inherits the bypass.
  File: packages/luca-core/src/verification/schemas.ts:14-49
  Suggestion: Add `.superRefine` to VerificationCriterionSchema rejecting `deferred === true && met === true` and `deferred === true && !deferredFollowUp`, making the documented invariant machine-enforced at the write gate.
  Cross-phase: false
- [SHOULD-FIX] validateVerificationRef reads verify.json with an unvalidated type cast (validate-verification-ref.ts:70-79) instead of VerificationResultSchema.safeParse, even though readVerificationResult (verification-result.ts:52) already does schema-validated reads. Because verify.json can be Write-authored (bypassing the handler's schema gate), loose typing creates spoofing slack: a truthy non-boolean `met` (e.g. `met: 1` or `met: "yes"`) passes the `!found.met` check at line 105, and `deferred: "true"` (string) evades the `deferred === true` check at line 95 while reading as deferred to humans. Strict `status !== 'PASS'` at line 117 is the only strictly-typed gate.
  File: packages/luca-cli/src/write-surface/helpers/validate-verification-ref.ts:70-79,95,105
  Suggestion: Parse with VerificationResultSchema.safeParse and return VERIFY_FILE_INVALID on failure (or reuse readVerificationResult); booleans then guaranteed boolean.
  Cross-phase: false
- [SHOULD-FIX] `luca claim-verify` echoes raw artifact-derived text to the terminal without control-char sanitization: `warning.sourceContext` (claim-verify.ts:71) is a raw line slice from the scanned file (claim-verifier.ts:416 — `raw.trim().slice(0, 240)` preserves ESC/ANSI bytes), and the `artifact-unreadable` evidence string (claim-verifier.ts:730) embeds the user-supplied filePath plus fs err.message, echoed at claim-verify.ts:63. A hostile artifact (e.g. a PR body fetched from a fork) can inject terminal escape sequences into the operator's terminal. Symbol/path/quantitative claim text is regex-constrained (IDENTIFIER_RE, FILE_PATH_RE charsets) and not exposed.
  File: packages/luca-cli/src/commands/claim-verify.ts:63,71
  Suggestion: Apply the sanitizeControlChars convention to `failure.evidence`, `failure.claim.text`, and `warning.sourceContext` before logging.
  Cross-phase: false
- [NOTE] FORBIDDEN_PHRASE_MATCHERS (claim-verifier.ts:342-345) interpolates each phrase into `new RegExp` with only space→`\s+` substitution and no regex-metachar escaping. Safe today (all five constants are letters+spaces) but latent regex-injection/break if a phrase with metachars is ever added to FORBIDDEN_LANGUAGE_PHRASES. Consider escaping non-space chars when building the matcher.
- [NOTE] Regex DoS review: DELIVERABLE_GRAMMAR (luca-plan-lint.ts:44-45) is worst-case polynomial (greedy `.+` backtracking across multiple ` → ` occurrences × linear ID-list match), not exponential — the ID-list tail requires a literal comma per iteration so adjacent `\s*` quantifiers cannot loop ambiguously. COMPOUND_CONNECTIVE, ABSOLUTE_QUANTIFIER, EVIDENCE_MARKER_RE, maskInlineCodeSpans are all linear. No catastrophic backtracking found.
- [NOTE] fsGrepFiles (claim-verifier.ts:504-556) follows symlinks via statSync — a symlinked directory can walk outside repoRoot and a symlink cycle loops until the timeout budget trips (line 514). Bounded by budget and advisory-only output, but `lstatSync` + skip-symlinks would be tighter.

Checks performed with no findings (evidence for the clean areas):
- Path traversal in the verify handler: slug is PhaseSlugSchema-validated in resolveActiveSlug (packages/luca-core/src/state/helpers/resolve-active-slug.ts:62) before phasePathFor (which throws on invalid slugs) — a crafted roadmap entry name cannot produce `../` in the write path (luca-phase-write-verify.ts:73-75).
- Prompt-injection in the doctrine constant: VERIFICATION_DOCTRINE (verification-doctrine.ts:11-38) interpolates only the compile-time FORBIDDEN_LANGUAGE_PHRASES constant from luca-core — no runtime values reach the instruction body.
- Command injection: git invocations use spawnSync with array args, no shell, `--fixed-strings`; needles are charset-constrained (IDENTIFIER_RE at claim-verifier.ts:220 starts with letter/underscore, singularized nouns are letters), so `-`-prefixed-arg smuggling is impossible (claim-verifier.ts:434-470).
- Echo sanitization in the two new handlers themselves: luca-phase-write-verify sanitizes Zod issue paths AND messages (lines 58-60); luca-plan-lint sanitizes the file path and err.message (lines 342, 351-353) and all finding messages interpolate only regex-captured token text (`connective`, `quantifier`, `refId` — charset-constrained by their patterns).
- deferred:true+met:true consumer sweep: both in-scope consumers reject it (validate-verification-ref.ts:93-104 checks deferred before met; aggregateVerificationResults at verification-result.ts:149,171 blocks regardless of met). Residual risk recorded in the schema SHOULD-FIX above.
- compliance enum spoofing: DeliverableComplianceSchema.compliance is z.enum(['shipped','missed','partial']) (schemas.ts:61), enforced at the write gate; no consumer in scope branches on a raw-string compliance read.

CONSOLIDATED:
  MUST_FIX_COUNT: 1
  SHOULD_FIX_COUNT: 3
  NOTE_COUNT: 3
  CROSS_PHASE_COUNT: 0
