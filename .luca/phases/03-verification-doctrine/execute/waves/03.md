# Wave 3 — deliverable manifest (plan-side)

## Task 1.3.1 (architect.ts) — executor attestation
- ac-09: `## Deliverables` template section :215 (D-line grammar + mapping rules); Criteria Quality Rule 4 "Deliverable tracing" :254; BOTH 150-line-budget exemption sentences name Deliverables — :38 and :442.
- tsc exit 0.

## Task 1.3.2 (luca-plan-lint.ts) — executor attestation
- ac-10.1: grep "Deliverables" hits :125,:131,:134,:142,:162,:251,:308,:337. Three new warn-only checks: MISSING_DELIVERABLES_SECTION, DELIVERABLE_GRAMMAR, DELIVERABLE_UNKNOWN_CRITERION; findVerificationCriteriaSection generalized to shared findSection; maskInlineCodeSpans applied to D-lines pre-match (decoy `code → span` did not skew).
- ac-10.2 (G-DX-001-safe): fixture at /tmp (outside .luca/) with D-line referencing ac-99 → warnings at :5 (unknown ac-99) + :6 (grammar); isError:false; exit 0; fixture deleted.
- anti-03: 2 warnings present, exit 0. Declared-ID rule: tombstoned + split-parent lines count as declared. tsc exit 0.
- Confidence (medium, design-choice, logged by orchestrator): unknown-criterion check skipped when VC section absent.

## Task 1.3.3 (plan-reviewer + mirrors) — executor attestation
- ac-11: plan-reviewer.ts :74 check `10. **Deliverables mapping**` (umbrella live targets, [SPLIT → …] parents invalid); grep "Deliverables" hits skills/phase-plan/index.ts :310,:365 and commands/phase-plan.ts :49.
- Destination check: architect.ts :215-219 template + verifier.ts :94 umbrella rule both present in same worktree delta.
- tsc exit 0. Wave close: `luca checks run` passed:true.
