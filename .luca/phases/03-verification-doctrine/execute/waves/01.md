# Wave 1 — tracer bullet (deferred-verify slice)

## Task 1.1.1 (schemas) — executor attestation
- ac-01.1: grep `deferredFollowUp` schemas.ts → :29 (JSDoc), :35 (field `z.string().optional()`).
- ac-01.2: `deferred: z.boolean().optional()`; JSDoc states deferredFollowUp REQUIRED when set + met:false until deferred probe runs.
- ac-01.3: probeType optional `z.enum(['file-read','grep-symbol','command','http','deploy','ui-screenshot','db-select','config-read'])` at :37.
- ac-02: DeliverableComplianceSchema :53, compliance enum `['shipped','missed','partial']` :61; `deliverables` optional array on VerificationResultSchema :108; barrel exports type DeliverableCompliance.
- ac-15: `bunx --bun tsc --noEmit` exit 0.

## Task 1.1.2 (doctrine constant) — executor attestation
- ac-03.1: file exists; shared/index.ts:13 re-exports VERIFICATION_DOCTRINE.
- ac-03.2: probe table grep `-c '^|'` = 10 (2 header/sep + exactly 8 rows: file write, code edit, command, HTTP/API, deploy, UI, schema/DB, config/env).
- ac-03.3: dual-evidence fallback line :25 (attestation in execute/waves/NN.md + independent structural probe, noted in verify.json notes, never attestation alone).
- ac-04.1/.2: all 5 phrases at :27 with "forbidden only WITHOUT attached probe evidence" framing.
- ac-16.1: :31 — `luca todo add` with source `deferred-verify:<slug>:<ac-id>` verbatim. ac-16.2: :32 cannot-flip-to-met statement.

## Task 1.1.3 (aggregate + todo gate) — executor attestation
- ac-05: verification-result.ts :149 blockingGaps filter `c.deferred === true || (!c.met && c.blocking)`; :171 `latest.criteria.every((c) => c.deferred !== true) &&` gates allCriteriaMet. Deferred-only-unmet AND deferred:true+met:true both ⇒ allCriteriaMet=false + blockingGaps entry.
- ac-06: CRITERION_DEFERRED declared :23, returned :97 BEFORE CRITERION_UNMET :107; runs on found criterion regardless of met (G-ARCH-002); message cites deferredFollowUp when present.
- ac-15: tsc exit 0. Wave close: `luca checks run` passed:true.
