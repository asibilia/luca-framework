# Wave 2 — doctrine embedding (execution-side bodies)

## Task 1.2.1 (verifier.ts) — executor attestation
- ac-07: `grep -c "luca verification write"` → 0 (was 3 at :24/:80/:122); VERIFICATION_DOCTRINE import :30, interpolation :81; Deliverable Compliance instructions :111-122 (deliverables[] per D-line, shipped|missed|partial derivation); verify.json example lists deliverables :136.
- Umbrella rule :94 — umbrellas LIVE with met derived from children, distinct from excluded [SPLIT → …] parents (G-SCOPE-001).
- New criterion fields in output example :105-107 (deferred/deferredFollowUp/probeType, enum values match schemas.ts).
- anti-05: no probe-table rows re-typed (grep negative); tsc exit 0.

## Task 1.2.2 (executor.ts) — executor attestation
- ac-08.1: VERIFICATION_DOCTRINE import :41, `${VERIFICATION_DOCTRINE}` interpolation :83 inside step-4 verify section.
- Self-Distrust Mandate aligned: evidence-in-same-block bullet + 5 forbidden phrases banned without attached probe output.
- anti-05: probe-table row strings grep negative; tsc exit 0.

## Task 1.2.3 (execute.ts + review.ts) — executor attestation
- ac-08.2: `grep -c "luca verification write"` execute.ts → 0; digest at :262 names canonical VERIFICATION_DOCTRINE in artifacts/shared/verification-doctrine.ts (5 rule-class lines, no re-typed table); :269 native Write at verify pipelineStep per STEP_ARTIFACTS (cross-checked step-artifacts.ts:50).
- ac-08.3: review.ts :82 deferred = OPEN GAP, never MET, PARTIAL/UNMET citing deferredFollowUp; coverage-table note :171; claim-verify tie-in :202.
- tsc exit 0. Wave close: `luca checks run` passed:true.
