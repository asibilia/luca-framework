# Wave 4 — finalize gate + write-surface hardening

## Task 1.4.1 (finalize.ts) — executor attestation
- ac-12.1: grep ReReadCheck → :151, :172 (Step 3 extension, same single Gap Report, resolution via existing re-entry path).
- ac-12.2: :155-158 source priority 1) GitHub issue body 2) roadmap phase goal (.luca/roadmap.md) 3) context.md decisions; asks checked against shipped work AND verify.json deliverables[] compliance; unjustified missed/partial blocks.
- ac-12.3: grep "criterionId, wave" → no matches; :469 `verificationRef: { criterionId }` (matches VerificationRefSchema todos/schemas.ts:51-58). Gap Audit item 4 hardened: deferred = blocking gap.
- tsc exit 0.

## Task 1.4.2 (luca-phase-write-verify.ts) — executor attestation
- ac-13.1: :8 `import { VerificationResultSchema } from '@alecsibilia/luca-core/verification'`.
- ac-13.2 (G-DX-001-safe): `bun -e` direct-schema probe safeParse({status:'BOGUS'}) → success:false with issues; grep :53 `VerificationResultSchema.safeParse(args.result)` runs before phasePathFor/writeAtomicFile (:73-75). Live handler NOT invoked.
- ac-13.3: grep `z.record(z.string(), z.unknown())` → nothing. Description rewritten to criteria[]/checks/status/recommendation/deliverables[] contract; errors sanitized per sanitizeControlChars convention. tsc exit 0.
- Confidence (high, design-choice, logged by orchestrator): inputSchema stays z.unknown(); safeParse in-handler.

## Task 1.4.3 (claim-verifier scan) — executor attestation
- ac-14: bun -e probes — prose "should work" flagged; backticked phrase NOT flagged; evidence-adjacent NOT flagged; fenced block NOT flagged; word-boundary "abandoned" NOT flagged. CLI runMain: warnings-only fixture → WARN forbidden-language printed, exit 0, "All claims verified". Failing-claim fixture with warning → exit 1 driven by claim failure only.
- FORBIDDEN_LANGUAGE_PHRASES exported from luca-core claim-verifier; report carries forbiddenLanguage[]; CLI prints warnings outside the exit-code branch (:67/:72 untouched by warnings).
- G-ARCH-001 RESOLVED in-phase: verification-doctrine.ts :9 imports FORBIDDEN_LANGUAGE_PHRASES from @alecsibilia/luca-core/claim-verifier, :30 interpolates; rendered line byte-identical to prior hand-listed text. Import direction luca-tools → luca-core only (workspace dep pre-existing).
- anti-01: cli.ts diff empty, claim-verify pre-registered :25-27, no new verb. anti-03: warnings never alter exit code. tsc exit 0.
- Confidence (medium, plan-gap, logged by orchestrator): evidence-marker heuristic (fence/prompt/keyword ±1 line).
Wave close: `luca checks run` passed:true.
