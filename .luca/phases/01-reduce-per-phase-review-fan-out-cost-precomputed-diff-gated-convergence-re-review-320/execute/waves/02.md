# Wave 2 — Mirror the gate (3 parallel tasks)

## Task 2.1.1 — modes/execute.ts (cross-reference mirror)

- Status: complete
- Added cross-reference note (+2 lines, single hunk) at Review Iteration Re-entry (~L412): pre-fix HEAD SHA stashed to `.luca/tmp/review-prefix-sha.json` by review mode's Route B; round-2 re-review diff-gated, skipped `only when provably safe` — `When in doubt, re-review`; Step 3 re-verify explicitly NOT gated. No gate algorithm here (mirror only). Step 4 fan-out untouched.
- Evidence: 3 required literals grep-confirmed; tsc exit 0; anti-04 clean; `luca rules run` 0 findings. Staged.

## Task 2.1.2 — skills/lu-review/index.ts (skill gate)

- Status: complete
- New "Re-run gate" section before "Run the reviewers": SHA read from `.luca/tmp/review-prefix-sha.json`; `git diff <pre-fix-sha> --name-only` ∪ `git ls-files --others --exclude-standard` with `.luca/` exclusion note (G-ARCH-003); empty-diff / zero-overlap skip; ambiguity → full re-review; post-skip backlog-capture + skip-reason note + advance to learn, never loops back. Aggregate loop-back now stashes `{"sha": "<HEAD>"}` before directing to `/phase-execute`.
- Design note (high confidence): lu-review body has no `reviewIteration` counter — gate applicability = "stash file exists" (functionally equivalent; file only written on the MUST-FIX loop-back path).
- Evidence: all 9 literals grep-verified; tsc exit 0; anti-04 clean; fan-out spawn block byte-identical. Staged.

## Task 2.1.3 — skills/phase-execute/index.ts (legacy gate)

- Status: complete
- Step 8.1 CRITICAL route stashes pre-fix HEAD SHA before the `--quality-fixes` exit; Step 8 gained a re-entry diff gate mirroring review.ts Step 3.5 (same union, same skip conditions, `.luca/` exclusion note); post-skip routing backlogs findings via `luca todo add --status backlog --source review-finding`, notes skip reason, jumps to step 9, never re-fires the loop.
- Design note (medium confidence): gate keyed on `--quality-fixes` re-entry (no `reviewIteration` counter in this body); anchored at Step 8 top after the no-changed-files early-exit.
- Evidence: all 9 literals grep-confirmed; anti-01/anti-03 prose intact (L912, L930, L1182); forbidden phrases 0 matches; tsc exit 0; `luca checks run` passed; anti-04 clean. Staged.
