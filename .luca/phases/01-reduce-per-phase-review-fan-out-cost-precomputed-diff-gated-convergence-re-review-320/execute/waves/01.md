# Wave 01 — Task 1.1.1

**Task**: Diff-gated round-2 re-review in review mode (`packages/luca-tools/src/artifacts/modes/review.ts`). Instruction-body-only change (#320).

## Changes Made

1. **Route B pre-fix SHA capture** (Step 7, Route B item 2): before `luca state advance --to-step execute`, the mode now instructs stashing `{"sha": "<HEAD>"}` (from `git rev-parse HEAD`) to `.luca/tmp/review-prefix-sha.json` via the native Write tool.
2. **New `### Step 3.5: Re-entry Diff Gate`** inserted before Step 4 (fractional numbering matches existing Step 4.5 / 5.5 convention). Active only when `reviewIteration > 0`. Computes `git diff <pre-fix-sha> --name-only` unioned with `git ls-files --others --exclude-standard` (with a G-ARCH-003 scoping note excluding pipeline-generated `.luca/` paths so the empty-diff branch stays reachable). Skips round-2 (the re-review fan-out; NOT re-verify) ONLY on `diff is empty` or `provable zero overlap` with prior MUST-FIX `File:line` cites; any overlap/parse failure/ambiguity → full round-2. Skip `only when provably safe`; `When in doubt, re-review`. Missing/unparsable SHA file → full round-2.
3. **Post-skip routing** (inside the gate): backlog-capture every unresolved MUST-FIX/SHOULD-FIX via `luca todo add --status backlog --source review-finding` (mirrors the Step 7 budget-exhausted mechanism), note the skip reason + pre-fix SHA in the audit artifact, then `luca state advance --to-step learn` — a skip exits the loop and NEVER re-enters Route B.

## Verification

- All 9 required literal tokens grep-confirmed present in the rendered body (grep -cF, each >= 1).
- Protected literals untouched: `5 reviewer subagents in parallel` count unchanged (1); no perspective/isolation prose modified.
- Forbidden Lever-1a/1b phrases absent: `single multi-perspective reviewer` = 0, `precomputed diff artifact` = 0.
- anti-04: `git status --porcelain packages/luca-core/` → empty.
- `bunx --bun tsc --noEmit` → exit 0.
- `luca rules run` → 0 findings.
- Confidence logged: high / design-choice (Step 3.5 naming + gate-embedded post-skip routing).

## Commit Status

**STAGED, NOT COMMITTED.** `git add packages/luca-tools/src/artifacts/modes/review.ts` succeeded (status `M `). `git commit -m "feat(review-gate): diff-gated round-2 re-review in review mode (#320)"` was BLOCKED by the stage-gate hook (bash-commit denied in pipelineStep=execute). Change left staged per instructions; orchestrator to commit post-EXECUTING.
