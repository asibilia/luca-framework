# Wave 1 — CLI init helper + repo docs

Status: complete (files staged; commit deferred to orchestrator — stage-gate
blocks `git commit` in EXECUTING).

## Tasks

- **1.1.1** `packages/luca-cli/src/init/helpers/ensure-compact-instructions.ts` —
  `ensureCompactInstructions(cwd, log)`, modeled line-for-line on
  `ensureLucaGitignore`. Idempotent header-guarded append to `<cwd>/CLAUDE.md`
  (creates if absent; skips when `## Compact Instructions` header already
  present, so re-runs never duplicate and user-edited copies are untouched).
  Block references lookup LOCATIONS only: `.luca/state.json`
  (`pipelineStep`/`currentPhase`/`sessionId`), `.luca/config.json`
  (`muninn.vault`), and the `session:phase-boundary-handoff` memory concept.
- **1.1.2** Exported the helper (+ header/block constants) from
  `init/index.ts`; imported + called it right after `writeProjectSkeleton` in
  the Step 5 flow in `commands/init.ts` (project cwd + `p.log.info` logger).
- **1.1.3** `ensure-compact-instructions.test.ts` (bun:test + mkdtemp, matching
  `write-project-skeleton.test.ts`). 4 cases: fresh append, append to existing
  file w/o header, re-run no-op (exactly one header), user-modified block
  untouched. 4 pass / 0 fail.
- **1.1.4** This repo's `CLAUDE.md` `## Compact Instructions` preserve-list:
  added explicit `pipelineStep` line, run id via `.luca/state.json` →
  `sessionId`, and recall of `session:phase-boundary-handoff`.

## Verification

- `bunx --bun tsc --noEmit` → exit 0.
- `bun test .../ensure-compact-instructions.test.ts` → 4 pass / 0 fail.
- `luca rules run` → 0 findings.
- greps: `ensureCompactInstructions` in index.ts + init.ts;
  `session:phase-boundary-handoff` in helper + CLAUDE.md; `sessionId` +
  `pipelineStep` in CLAUDE.md.
