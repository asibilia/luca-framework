# Wave 4 — review fixes (cycle 2), tasks 4.1 → 4.2 sequenced (single executor)

| Task | Status | Criteria | Files |
|---|---|---|---|
| 4.1 SEC-01 + SEC-02 | ✓ | ac-18 ✓ (tsc 0), ac-19 ✓ (JSON.stringify at list:58; TodoAreaSchema at schemas.ts:44, barrel, all 3 handlers), ac-20 ✓ (source-run `--area 'x"y'` → "must be kebab-case" exit 1) | schemas.ts, todos/index.ts, luca-todo-list.ts, luca-todo-add.ts, luca-todo-update.ts |
| 4.2 AR-01 + warning consolidation | ✓ | ac-21 ✓ (tsc 0 authoritative + checks gate passed), ac-22 ✓ (TodoPriority.options ×3, zero literal arrays), ac-23 ✓ (exactly 1 full-replace statement per file, names priority+area), ac-24 ✓ (--help probe: warning once, real field descriptions) | todo.ts, luca-todo-update.ts |

Advisories folded: G-ARCH-002 (shared TodoAreaSchema export instead of 4 regex copies — TodoPriority precedent), G-DX-006 (ac-22 as ≥3 + no-literal-array), G-SCOPE-003 (all 9 distinct stored area values probe OK against the regex; injection value REJECTED).

Deviations:
- "free-form" → "kebab-case" prose updates in handler/CLI descriptions (constraint changed; leaving prose would be drift). Logged high/design-choice; review hint: kebab-only acceptable?
- G-DX-005 (plan frontmatter criteria: 24 → actual 25 incl. ac-10b) NOT applied — plan.md is not a legal write at execute step; verifier should count by criterion ID, not frontmatter. Carry to finalize/docs commit if worth fixing.
- Ops note: `luca checks run --file` payload is a bare JSON array `[{argv,label}]`, not `{commands:[...]}` — flag help text misleading (candidate dx follow-up).
