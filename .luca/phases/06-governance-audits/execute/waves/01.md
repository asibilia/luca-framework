# Wave A — REQ-08 governance floors audit + registry

## Task 1.A.1 (docs/decisions/governance-floors-audit.md) — executor attestation
- ac-01: file exists. ac-02: grep gate-file basenames = 11 (≥8). ac-08: "Excluded scope" ×1 + "vault:init" ×3.
- anti-03: every cited `luca` verb/flag grep-verified real against source (full list in envelope: STAGE_TOOL_MATRIX, PIPELINE_TRANSITIONS, STEP_ARTIFACTS/WRITE_COMMAND_PHASES, BUDGET_BY_COMPLEXITY, classifier ALWAYS_DENIED/pipe-to-shell, confidence gate, rules gate exit-1, computePostmortemExitCode + `luca retro` exit-code gate [NO postmortem-gate subcommand], oversight enum, /lu + /phase-plan + /gh-pr-address flags). NO phantom verbs cited.
- Soft seam noted: rule-gate exit code is mechanically HARD but orchestrator-honoring is prose-enforced.
- skip-verify (folded 1.A.3): documented standalone-skill-only; /lu pipeline has no verify bypass.

## Task 1.A.2 (relaxation-paths.ts) — executor attestation
- ac-03: RELAXATION_PATHS=1. ac-04: RelaxationPath=2. ac-05: `floor:'(hard|soft)'`=13 (≥5; 12 entries = 5 hard, 7 soft).
- ac-06.1: skip-verify=1. ac-06.2: standalone=2 (verify-skip-standalone entry).
- anti-02: zod/safeParse/.parse(=0 (static const). anti-01: defineCommand=0. ac-09: tsc exit 0.
- All 9 source paths + flags grep-confirmed real.
