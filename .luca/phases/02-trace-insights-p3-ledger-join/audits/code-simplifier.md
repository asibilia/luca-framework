PERSPECTIVE: simplification
VERDICT: REQUEST_CHANGES
FINDINGS:
- [MUST-FIX] Proportional cost allocation never states its denominator, leaving a double-count reading open. "allocate each joined root run's `total_cost` proportionally across ALL step intervals its window overlaps, by wall-clock overlap fraction" (index.ts:143) admits two readings: (a) fraction = overlap ÷ total window duration (remainder to tail — conservation holds), or (b) fraction normalized over the sum of overlaps (full cost split among intervals). Under (b), the very next sentence — "Window portions that fall outside every known interval go to the unjoined tail as unallocated cost" — adds tail cost ON TOP of a fully-allocated total, so attributed + tail > total_cost. The parenthetical about millisecond-to-second intervals vs multi-minute turns makes reading (b) catastrophic, not marginal. An executor implementing sentence 1 before reading sentence 2 double-counts the headline metric.
  File: packages/luca-tools/src/artifacts/skills/trace-insights/index.ts:143
  Suggestion: Add one clause: "overlap fraction = (overlap duration with that interval) ÷ (the run's total window duration) — allocations plus the unallocated tail portion always sum to exactly `total_cost`." The existing test anchors ('by wall-clock overlap fraction', 'degrades exactly to full-cost-to-that-interval') survive the edit.
  Cross-phase: false
- [MUST-FIX] The unjoined-tail reason taxonomy contradicts the data it must classify, and drifts from the A5 skip-reason taxonomy. Stage D (index.ts:215) says the tail — "count of unjoined root runs and total unallocated cost (including window portions of joined runs...)" — is "broken down by reason — repo checkout not local, `.luca/` missing or unreadable, no interval overlap". A joined run's out-of-interval portion fits NONE of the three reasons: its repo was local, `.luca/` was readable, and it DID overlap an interval (that's why it joined). Forcing it into "no interval overlap" states a falsehood about a joined run. Separately, A5's graceful-skip list (index.ts:145) is a 3-way taxonomy (missing checkout / missing `.luca/` directory / unreadable ledger-or-telemetry files) while D merges the latter two, and the skip info renders in two places (Pipeline Attribution "listed with the skip reason" at index.ts:212 AND the tail breakdown at :215) — duplicated directive that will drift.
  File: packages/luca-tools/src/artifacts/skills/trace-insights/index.ts:145,212,215
  Suggestion: Define one canonical reason enum once in A5 (e.g. checkout-not-local, luca-dir-missing-or-unreadable, no-interval-overlap, joined-partial-window) and have both Stage D surfaces reference it; the joined-partial-window bucket carries the unallocated cost of joined runs.
  Cross-phase: false
- [MUST-FIX] The degraded-tuple premise is factually false: ledger `mode-transition` rows DO carry a runId. index.ts:141 asserts "ledger `mode-transition` rows carry no runId/slug/wave, so the joined tuple is `(runId: null, ...)`" — but `LedgerEntrySchema` requires `runId: z.string()` on every entry (packages/luca-core/src/ledger/schemas.ts:17-18, "correlates ledger entries with telemetry records") and the writer stamps it from `state.sessionId` (packages/luca-cli/src/write-surface/handlers/luca-state-advance.ts:262-272). An executor reading a real ledger sees runId on every row while the spec says none exists. Worse, the false premise motivates unnecessary indirection: the "nearest-in-time slug-bearing telemetry record" heuristic replaces the direct deterministic lookup runId → `.luca/telemetry/<runId>.jsonl` → `slug` field (telemetry schema packages/luca-core/src/telemetry/schemas.ts:53-59).
  File: packages/luca-tools/src/artifacts/skills/trace-insights/index.ts:141
  Suggestion: Correct the premise: rows carry runId (nullable in the tuple only when the stamped value is the empty string). Slug resolution order: (1) slug from any record in `.luca/telemetry/<row.runId>.jsonl` overlapping the interval, (2) the existing nearest-in-time heuristic, (3) unavailable-with-note. Removes a heuristic where a key join exists.
  Cross-phase: true
- [MUST-FIX] Runs with null `end_time` (pending/orphaned) make the join window `[start_time, start_time + duration)` undefined — and `duration` is not even a selected field. A2's select list (index.ts:99-101) fetches `start_time`/`end_time`; A5 (index.ts:137,143) computes with an underived `duration` and gives no rule for `end_time: null`, which A3 (index.ts:116) says really occurs (pending runs older than 1h). A deterministic script hits NaN arithmetic → silent corruption of `costByPipelineStep`, a crash, or an undocumented drop, depending on the executor's guess.
  File: packages/luca-tools/src/artifacts/skills/trace-insights/index.ts:137,143
  Suggestion: Define `duration = end_time − start_time` explicitly and add: runs with null `end_time` are excluded from the join and routed to the unjoined tail under a named reason (folds into the MF-2 reason enum).
  Cross-phase: false
- [SHOULD-FIX] The telemetry-preferred interval path has no tuple rule. The degraded tuple (index.ts:141) is scoped to "the ledger-fallback path", and Stage C (index.ts:176) defers to "the degraded-tuple rule" — but when telemetry `mode.start`/`mode.end` intervals ARE used (the preferred branch, index.ts:139), nothing defines runId/slug/wave for the tuple, even though telemetry records carry all three natively (packages/luca-core/src/telemetry/schemas.ts:53-61). The branch is currently unreachable ("real telemetry files contain zero such records"), so advisory — but a binding rule with an unspecified preferred branch is a trap for the first executor after telemetry starts emitting.
  File: packages/luca-tools/src/artifacts/skills/trace-insights/index.ts:139,141,176
  Suggestion: One sentence: "On the telemetry-sourced path the tuple is populated directly from the record's `runId`/`slug`/`wave` fields — the degraded tuple applies only to the ledger fallback."
  Cross-phase: true
- [SHOULD-FIX] Duplicated A5 output that can drift: bullet 4 ("Per-phase `review.iteration` count → Stage B pool rule 7", index.ts:152) is a strict subset of bullet 3 (`reviewIterationsVsCost` "per-phase `review.iteration` count paired with ... cost", index.ts:151). Two independently-worded statements of the same count invite divergence (e.g. one later gains a filter the other lacks).
  File: packages/luca-tools/src/artifacts/skills/trace-insights/index.ts:151-152
  Suggestion: Drop bullet 4 and amend bullet 3's consumer list: "→ Stage D review-convergence cost trajectory; its iteration count also feeds Stage B pool rule 7." Test index.test.ts:318 (anchors bullet 3) survives; no test pins bullet 4.
  Cross-phase: false
- [NOTE] index.ts:139 says "step = the row's `to` value", but per `LedgerEntrySchema` (data: free-form record, packages/luca-core/src/ledger/schemas.ts:22) and the writer (luca-state-advance.ts:271 `data: { from, to }`), the step is `data.to`, nested — worth phrasing precisely so the script author doesn't grep the top level.
- [NOTE] The ledger-fallback recipe (interval = [row N ts, row N+1 ts), index.ts:139) yields N−1 intervals from N rows: all time after the final mode-transition deterministically lands in the unjoined tail. Cost is conserved, but the currently-active step is systematically unattributed; an explicit sentence (and the MF-2 reason bucket) would make this visible rather than surprising.
- [NOTE] Pre-existing (outside the P3 diff): Stage A4 fetches child runs only "for the top candidate traces (see Stage B pool)" (index.ts:123) while Stage B pool rules 5–6 (compaction events, loop signature, index.ts:163-164) are computed FROM A4 output — a bootstrap circularity the text never resolves (presumably rules 1–4 seed the candidates). Not introduced by this phase; flagging for a future pass.

CONSOLIDATED:
  MUST_FIX_COUNT: 4
  SHOULD_FIX_COUNT: 2
  NOTE_COUNT: 3
  CROSS_PHASE_COUNT: 2
