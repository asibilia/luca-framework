# Wave 1 — schema substrate (luca-core)

## Task 1.1.1 + 1.1.2 (telemetry/schemas.ts + index.ts) — executor attestation
- ac-01: grep -c kinds = 7 (≥3); signal.satisfaction, signal.failure-dump, classifier.override added to TelemetryKind union (additive; `kind: z.string()` open validation untouched).
- ac-02: OverrideSourceSchema = z.enum(['cli-flag','force-complex','human-ask','heuristic-promotion']) present (:97-100).
- ac-03: grep -c MetaSchemas = 9 (≥3); SatisfactionSignalMetaSchema/ClassifierOverrideMetaSchema/FailureDumpMetaSchema, `.passthrough()` advisory shapes, exported from index.ts.
- anti-01: git diff shows NO change to TelemetryRecordSchema field block (additive only).
- anti-04: grep `MetaSchema.parse(` = 0 (no throwing wiring).
- ac-16: tsc exit 0. Wave close: `luca checks run` passed:true.
