# Wave 4 — REQ-04 synthesis + readback

## Task 1.4.1 (learner.ts) — executor attestation
- ac-13: grep -i `cluster|theme|synthesi` = 5 exit 0. Added Step 1b: clusters orchestrator-injected `<signal-digest>` (signal.* telemetry + confidence journal) into a `## Signal Synthesis` learn.md section (recurring failures, valence trends by step/source).
- anti-06: no NEW telemetry/MCP fetch; explicit "digest is orchestrator-injected, learner has no Bash/MCP" framing.

## Task 1.4.2 (lu/index.ts learn step) — executor attestation
- ac-14: grep -E `<signal-digest>|signal-digest` exit 0 (:199,:202,:208). Added "Learner prompt injection" section mirroring `<confidence-gate-resolutions>` pattern at :130-142; orchestrator gathers run signal.* + confidence journal, injects `<signal-digest>` block before spawning learner.
- anti-02: only lu/index.ts (+22/-1).

## Task 1.4.3 (session-resume/index.ts) — executor attestation
- ac-15: grep -i `signal` exit 0. Added readback step: replays run `signal.*` telemetry (tally by kind) + surfaces prior learn.md Signal Synthesis themes on resume.
- ac-16: tsc exit 0. Wave close: `luca checks run` passed:true.
