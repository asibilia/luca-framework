# Wave 5 — review fix cycle

Review found split verdict: arch+sec APPROVE; simp+dx REQUEST_CHANGES with 2 MUST-FIX (recurring phantom-capability/field-drift class — verifier's token-greps missed them).

## MUST-FIX 1 — emit examples omitted required `--run-id`
`luca telemetry emit` declares `run-id` REQUIRED (telemetry.ts:32-36); all emit examples would exit-1. Fixed (lu/index.ts): establish run id once at Step 0 — `RUN_ID=$(luca state read | jq -r '.sessionId // empty'); [ -z "$RUN_ID" ] && RUN_ID=$(luca telemetry new-run)`; pass `--run-id "$RUN_ID"` on all 7 emits; file `.luca/telemetry/$RUN_ID.jsonl`.

## MUST-FIX 2 — `runId` field doesn't exist; it's `sessionId`
state schema has `sessionId` (the run id, schemas.ts:93-96), not `runId`. Digest-collection + session-resume readback (jq `.runId`) read a nonexistent field → ac-15 was non-functional. Fixed: use sessionId consistently; emit and readback line up on the same run id.

## Follow-up fix (orchestrator-caught) — sessionId can be UNSET
Verified `luca state read` → sessionId unset in this recovery context (only stamped at init). "use sessionId as --run-id" would pass empty → emit exit-1. Closed: lu falls back to `luca telemetry new-run`; session-resume skips readback gracefully when unset.

## SHOULD-FIX
- session-resume `signal.failure` → `signal.failure-dump` doc drift fixed (jq prefix `signal.` filter retained).
- `--meta` JSON safety prose added: keep inline fields single-line/quote-free; push risky content to `.luca/tmp/<kebab>.json` via `meta.dumpRef`.

## Evidence
7 emits all carry `--run-id`; fallback `telemetry new-run` present (3 hits); all ac tokens intact (classifier.override, signal.satisfaction×4, source:'gate-ask'/'outcome', dumpRef×6, signal.failure-dump×5, `<signal-digest>`×2); ac-15 grep signal session-resume exit 0; anti-02/03 hold; tsc exit 0; `luca checks run` passed.
