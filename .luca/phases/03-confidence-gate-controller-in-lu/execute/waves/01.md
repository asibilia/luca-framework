# Wave 01 — Confidence Gate Controller in /lu

## Tasks completed

### Task 1 — Confidence Gate sub-step in the `lu` SKILL (`packages/luca-tools/src/artifacts/skills/lu/index.ts`)

- Added `### Confidence Gate (between plan-review and execute)` sub-step after the `plan-review` pipeline table row and before `execute`.
- Gate sub-step runs `luca confidence gate --slug <currentPhaseSlug>`, routes `auto` silently, spawns `researcher` per `research` entry (`[gate-research]`), surfaces one targeted question per `ask` entry (`[gate-ask]`).
- Resolutions persist to `.luca/phases/<slug>/plan-review.md` as `## Confidence Gate Resolutions` (legal at `plan-review` step; `context.md` explicitly prohibited).
- Resolutions held in-context and injected into the executor prompt as `<confidence-gate-resolutions>` block at the `execute` step.
- Updated `### Oversight` section: `full-auto` redefined as "autonomous — only pauses are confidence-gate `ask` items and CRITICAL safety"; `checkpoint` and `human-in-loop` updated accordingly.

### Task 2 — Mirror gate sub-step into the `/lu` COMMAND (`packages/luca-tools/src/artifacts/commands/lu.ts`)

- Added `*(gate)*` row to the pipeline loop table between `plan-review` and `execute`.
- Added `## Confidence Gate (between plan-review and execute)` section with concise 6-step mirror of the skill's gate logic.
- Updated `## Oversight` section with identical full-auto/checkpoint/human-in-loop redefinitions.

### Task 3 — Redefine `full-auto` + de-stale "(future)" refs

- `packages/luca-tools/src/artifacts/modes/triage.ts`: Updated oversight table — `full-auto` now reads "Autonomous — the only pauses are confidence-gate `ask` items (low-confidence + unresearchable) and CRITICAL safety."
- `packages/luca-tools/src/artifacts/modes/execute.ts`: Updated `## Checkpoint Interaction` — `full-auto` now explicitly states gate `ask` items were resolved at plan-review and injected into this prompt; no re-asking during execution.
- `packages/luca-tools/src/artifacts/modes/architect.ts`: Changed "(future) plan→execute confidence gate" → "**active** plan→execute confidence gate that runs after plan-review completes and before execution begins."
- `packages/luca-tools/src/artifacts/skills/phase-plan/index.ts`: Added "These entries feed the **active** confidence gate that runs after plan-review and before execute begins." to the confidence emission pointer.

## Verification results

- `bunx --bun tsc --noEmit`: PASSED (clean, no errors)
- `bun run build`: PASSED (agents:10 subagents:10 commands:17 skills:41 hooks:3 rules:0) — counts match spec
- Grep checks: all required strings present in lu skill + command; full-auto redefined consistently across all 4 surfaces; no "(future)" gate qualifier remains
- No luca-core files touched by this phase (pre-existing `packages/luca-core/src/confidence/` changes are from Phase 1/2)
- Smoke test: `luca confidence log --phase smoke-gate ... --confidence low` → `luca confidence gate` → `ask` bucket non-empty. Throwaway journal entry cleaned up (file deleted).
- Stage-gate note: appending `## Confidence Gate Resolutions` to `plan-review.md` was blocked at the current `pipelineStep=execute` — correctly, the orchestrator must do this at runtime during the `plan-review` step, not during this execution phase. This is by design.

## Deviations

- **Minor (noted):** `luca confidence log --score` flag (from executor system prompt step 3) does not exist in the v13 CLI surface. The confidence journal only supports the per-entry schema. No action taken; this is an executor-prompt artifact that references a future surface.
- **Minor (noted):** The plan-review.md `## Confidence Gate Resolutions` append was blocked by stage-gate (`pipelineStep=execute`, write is only legal at `plan-review`). This is correct behavior — the orchestrator appends this section at runtime. Not a code deviation.
- **Note:** Phase-plan "(future)" text was not present as a standalone string; instead the confidence emission pointer at line 270 was updated to explicitly state the gate is now "active". Net effect is identical.

## Follow-ups (from plan)

- `luca confidence resolve` command (mark journal entries resolved) — noted, not built.
- `--score` flag for executor-level confidence logging — noted gap; not in scope.
