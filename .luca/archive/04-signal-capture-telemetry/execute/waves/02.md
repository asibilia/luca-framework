# Wave 2 — REQ-05 classifier-override emit

## Task 1.2.1 (lu/index.ts Triage) — executor attestation
- ac-04: grep `classifier.override` lu/index.ts exit 0 (emit wired on heuristic-vs-final mismatch).
- ac-05: grep -E `luca classify --task .* --json` exit 0 (full invocation; --task required per classify.ts:30-35).
- anti-02: only lu/index.ts edited (no CLI change). anti-03: `telemetry emit`, `classify --task … --json` verified against telemetry.ts/classify.ts.
- meta {classifier:'complexity', from, to, source}; taxonomy cli-flag|force-complex|human-ask|heuristic-promotion.

## Task 1.2.2 (luca-telemetry-report/index.ts) — executor attestation
- ac-06: grep `classifier.override` = 4 matches; added to known-kind list + Step-3 byOverrideSource tally + Step-6 summary line.
- ac-16: tsc exit 0. Wave close: `luca checks run` passed:true.
