# Wave 3 — REQ-04 capture (satisfaction + failure dumps)

## Task 1.3.1 + 1.3.2 (lu/index.ts, serial) — executor attestation
- ac-07: grep `signal.satisfaction` exit 0 (3 occurrences).
- ac-08.1: grep -E `source:\s*'gate-ask'` exit 0 (NEW meta literal, distinct from pre-existing `[gate-ask]` annotation). ac-08.2: grep `oversight-pause` exit 0.
- ac-09: grep -E `source:\s*'outcome'` exit 0 — implicit-outcome emit PRIMARY (full-auto sparsity guarantee; gate resolution).
- ac-10: grep `signal.failure-dump` exit 0 (verify-escalate / checks-fail-exhausted / subagent-crash). ac-11: grep `dumpRef` exit 0 (large dumps → .luca/tmp/<kebab>.json).
- anti-02/03/05: only lu/index.ts; verbs verified; dumps in gitignored .luca/tmp/ (no contract entry).

## Task 1.3.3 (luca-telemetry-report/index.ts) — executor attestation
- ac-12.1: grep `signal.satisfaction` exit 0. ac-12.2: grep `signal.failure-dump` exit 0. (wave-2 classifier.override preserved.)
- ac-16: tsc exit 0. Wave close: `luca checks run` passed:true.
