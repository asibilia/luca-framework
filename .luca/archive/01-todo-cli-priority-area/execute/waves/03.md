# Wave 3 — backfill (orchestrator-only)

| Task | Status | Criteria | Notes |
|---|---|---|---|
| 3.1 backfill pai-review todos | ✓ | ac-17 ✓ (direct read of fresh engram shows top-level `"priority"`/`"area"` and no `[priority:` line in body; batch returned ok ×10) | muninn_remember_batch, 10 payloads, full resend (title/body/status/source preserved; prefix line stripped where present) |

Deviations:
- **Scope: 10 todos, not 9.** Plan drafted before the 10th pai-review todo (the phase's own CLI-fix todo) existed. All 10 backfilled; CLI-fix todo got a STATUS NOTE in body pointing at this phase.
- **Mechanism: direct muninn_remember_batch instead of per-todo `luca todo update`.** Rationale: `luca todo update` only emits the same muninn_remember instruction (no extra validation beyond TodoSchema, which wave-2 probes already exercised end-to-end via ac-08/09/10b); per-todo CLI route required staging 2-4KB bodies through argv (quoting hazard ×10) or .luca/tmp files (contract is .json-only — Write of .txt correctly BLOCKED by stage-gate). Payload shape mirrors the CLI's emitted canonical content exactly (schemaVersion/id/title/body/status/priority/area/source/updatedAt).
- satisfaction-signal-capture todo had no `[priority|area]` prefix (predates the convention) — fields assigned from REQ table (high/learning), body unchanged.
- Semantic recall lags fresh writes (~index delay); ac-17 verified via direct muninn_read by ID instead.
