# Wave 2 — surface integration + prose (2.1 → 2.2 sequenced; 2.3/2.4/2.5 parallel)

| Task | Status | Criteria | Files |
|---|---|---|---|
| 2.1 handlers accept priority/area | ✓ | ac-05 ✓ (tsc 0), ac-06 ✓ (fields in both inputSchemas; "full-replace" in update at lines 31/51/57) | luca-todo-add.ts, luca-todo-update.ts |
| 2.2 todo CLI flags + list + reject wiring | ✓ | ac-07 ✓ (tsc 0), ac-08 ✓ (--priority enum + --area in add --help; full-replace in update --help), ac-09 ✓ (--bogus-flag exit 1 named; --priority wrong → citty CLIError), ac-10 ✓, ac-10b ✓ (list --area x --priority high exit 0, instruction names `content.priority === "high" && content.area === "x"`) | todo.ts, luca-todo-list.ts |
| 2.3 wire reject into 11 non-todo leaves | ✓ | ac-11 ✓ (tsc 0), ac-12 ✓ (grep count 11 leaves + todo.ts = 12; source-run `state read --bogus` exit 1 named; no-flag + declared-kebab-flag probes pass) | branch, checks, confidence, phase, pr-review, preferences, repo, roadmap, state, verification, workflow .ts |
| 2.4 prose drift — skills | ✓ | ac-13 ✓ (luca-tools tsc clean; repo-wide re-verified 0 post-2.3), ac-14 ✓ (no "Todo File Format"/"--format json"/metadata-file priority pattern; first-class flags in gh-issue-triage:72-78) | todo-add, session-plan, gh-issue-triage skill index.ts (note/index.ts verified — no change needed) |
| 2.5 prose drift — modes/commands | ✓ | ac-15 ✓ (re-verified post-2.3), ac-16 ✓ (priority/area in todo-check field list line 26 + render line 34) | todo-check.ts (research.ts:257 verified exact enum match — no change) |

Deviations:
- **2.3 minor (logged, medium/plan-gap):** task-1.2 helper signature `cmd: CommandDef<ArgsDef>` failed at all call sites (citty CommandDef invariant in T via setup). Fixed via generic widening `<TArgsDef extends ArgsDef>` in run-handler.ts. Alternative (casts at 22 call sites) rejected. Flag to reviewer.
- Transient mid-wave tsc failures observed by 2.4/2.5 were races against 2.3's in-flight fix; repo-wide gate re-verified green (exit 0) after wave settled.
- 2.4: dropped `--metadata-file` from gh-issue-triage entirely (carried only priority/area — plan's keep-condition unmet). 2.1: full-replace note also added to priority/area describes (same drop-on-omit semantics).
