PERSPECTIVE: dx
VERDICT: REQUEST_CHANGES
FINDINGS:
- [MUST-FIX] Instruction bodies cite `luca claim-verify` invocation forms that do not exist in the shipped CLI. The CLI (new this phase) is `luca claim-verify <file> [--repo-root]` (packages/luca-cli/src/commands/claim-verify.ts:20-44; registered in cli.ts:25-27 with only positional `file`). But the agent-facing instructions tell the LLM to run: `luca claim-verify verify-text --text "<full review output>"` (packages/luca-tools/src/artifacts/modes/review.ts:205), `luca claim-verify verify-file --path <planFile>` (packages/luca-tools/src/artifacts/modes/finalize.ts:200), and `luca claim-verify gate --paths ... --texts ...` (finalize.ts:310, finalize.ts:467). finalize.ts:313 also tells the agent to branch on a structured return `code: CLAIM_VERIFICATION_FAILED`, but the CLI emits logger lines and exit code 0/1 only — no structured code. Every one of these invocations fails at runtime; the review self-check and the pre-PR claim gate (the phase's headline verification features) are unreachable as instructed.
  File: packages/luca-tools/src/artifacts/modes/review.ts:205; packages/luca-tools/src/artifacts/modes/finalize.ts:200,310,313,467
  Suggestion: Either (a) rewrite all four instruction sites to the shipped form — write the text to a repo-scoped temp file under `.luca/tmp/` and run `luca claim-verify <file>`, branching on exit code 0/1 — or (b) extend the CLI with the documented subcommands. Pick one contract and make instructions and CLI agree.
  Cross-phase: true
- [MUST-FIX] The [DEFERRED-VERIFY] protocol step 2 in VERIFICATION_DOCTRINE instructs "Create the tracked follow-up via `luca todo add`" (packages/luca-tools/src/artifacts/shared/verification-doctrine.ts:36), and the doctrine is interpolated into the verifier and executor SUBAGENT bodies (subagents/verifier.ts:81, subagents/executor.ts:83). But `luca todo add` persists nothing itself — it returns a `mcp__muninn__muninn_remember` instruction the caller must execute (packages/luca-cli/src/write-surface/handlers/luca-todo-add.ts:60-117), and subagents explicitly have NO MCP access (SUBAGENT_SHARED_PREFIX / "Memory I/O Is the Orchestrator's Job"). Result: a subagent following the doctrine records `deferredFollowUp: "<todo-id>"` in verify.json while the todo itself is silently never persisted — the "tracked follow-up" that CRITERION_DEFERRED gating depends on does not exist. validateVerificationRef only checks the id string in verify.json, so nothing catches the loss.
  File: packages/luca-tools/src/artifacts/shared/verification-doctrine.ts:34-37
  Suggestion: Amend doctrine step 2 to the subagent reality: "run `luca todo add ...`; you cannot execute the returned muninn_remember instruction — include it verbatim in your final structured output so the orchestrator persists it." Alternatively move follow-up creation to the orchestrating mode-agent (which has MCP) and have the subagent only emit the deferred criterion.
  Cross-phase: true
- [SHOULD-FIX] Stale documentation in the new `luca plan` command group: both the module header ("The four regex checks (missing ac-NN / anti-NN IDs, compound criteria, absolute quantifiers..., missing anti-criteria)", packages/luca-cli/src/commands/write-surface/plan.ts:12-17) and the `lint` subcommand description (plan.ts:30-36) enumerate only 4 checks, but the handler implements seven (a–g), including the three deliverable-manifest checks (packages/luca-cli/src/write-surface/handlers/luca-plan-lint.ts:138-176). An agent reading `luca plan lint --help` will not know deliverable warnings exist.
  File: packages/luca-cli/src/commands/write-surface/plan.ts:12-17,30-36
  Suggestion: Update both strings to enumerate all seven checks (or say "seven regex checks keyed to the criterion and deliverable grammars" and point at the handler doc), mirroring the accurate description already in lucaPlanLintTool.description (luca-plan-lint.ts:336-337).
  Cross-phase: false
- [SHOULD-FIX] Alert-fatigue risk in `scanForbiddenLanguage`: the phrase set includes `done` and `expected to` (packages/luca-core/src/claim-verifier/claim-verifier.ts:324-330) matched with bare word boundaries, and the evidence window is only ±1 raw line (claim-verifier.ts:406-410). Ordinary status prose — "marking the task as done", "all waves done", "this is expected to be set by the orchestrator" — warns even when probe evidence sits two lines away. Advisory-only is the right call, but a warning stream dominated by false positives trains agents to ignore the real confidence-theater hits (`luca claim-verify` prints every one, commands/claim-verify.ts:68-73).
  File: packages/luca-core/src/claim-verifier/claim-verifier.ts:324-330,406-410
  Suggestion: Either widen the evidence window (±2–3 lines or same-paragraph), or constrain the highest-noise phrases (`done`, `expected to`) to claim-shaped contexts (e.g. require them on a line that also matches a criterion/claim marker). Document the expected false-positive rate in the scanForbiddenLanguage JSDoc either way.
  Cross-phase: false
- [SHOULD-FIX] `validateVerificationRef` casts the parsed JSON with `parsed as {...}` instead of running `VerificationResultSchema.safeParse` (packages/luca-cli/src/write-surface/helpers/validate-verification-ref.ts:70-79), violating the repo's schema-first rule, and as a consequence the `VERIFY_FILE_INVALID` error code is misleading: it fires only on JSON syntax errors (line 60-68), never on schema-invalid content — a structurally bogus verify.json instead surfaces as `CRITERION_NOT_FOUND` with "Existing ids: (none)", which sends the agent hunting for the wrong problem. The schema is already imported one file over (handlers/luca-phase-write-verify.ts:8,53).
  File: packages/luca-cli/src/write-surface/helpers/validate-verification-ref.ts:60-79
  Suggestion: safeParse with VerificationResultSchema; on failure return VERIFY_FILE_INVALID with the issue list (same per-issue formatting as luca-phase-write-verify.ts:55-62). Type the rest of the function from the inferred schema type, dropping the manual cast.
  Cross-phase: false
- [SHOULD-FIX] The doctrine's probe table and the schema's `probeType` enum are parallel but never explicitly mapped. The table rows are artifact types ("file write", "schema/DB", "config/env"; packages/luca-tools/src/artifacts/shared/verification-doctrine.ts:15-26) while the enum tokens are probe kinds (`file-read`, `db-select`, `config-read`; packages/luca-core/src/verification/schemas.ts:37-48). An LLM filling in `probeType` must guess the mapping (is "deploy → live version string" `deploy` or `http`?); the only place the enum is spelled out is a one-line comment in the verifier's JSON example (subagents/verifier.ts:107).
  File: packages/luca-tools/src/artifacts/shared/verification-doctrine.ts:15-26
  Suggestion: Add a third column to the doctrine table with the canonical `probeType` token per row, so the doctrine (the single source of truth) carries the enum mapping every consumer needs.
  Cross-phase: false
- [NOTE] JSDoc gaps on new exports (mandatory-documentation rule): `FailureReason` and `ClaimFailure` (claim-verifier.ts:52-63) and `ValidateVerificationRefOptions` / `ValidationError` (validate-verification-ref.ts:12-28) ship without doc comments, while sibling exports in the same files are documented.
- [NOTE] Symbol-failure evidence hardcodes "git grep" — `evidence: 'git grep for "X" returned 0 hits.'` (claim-verifier.ts:621) is emitted even when the fs-walk fallback ran (`useGit === false`), which is mildly misleading when debugging a non-git environment.
- [NOTE] In the executor body, the interpolated `## Verification Doctrine` H2 lands between numbered protocol steps 4 and 5 (subagents/executor.ts:81-84), visually terminating the numbered list mid-protocol; steps 5–6 then resume after a full doctrine section. Consider interpolating after step 6 with a back-reference at step 4, as the verifier body does (doctrine after the protocol sections, verifier.ts:81).
- [NOTE] Error-message quality elsewhere is good and was checked, not assumed: CRITERION_DEFERRED includes the follow-up todo id and the resolution condition (validate-verification-ref.ts:95-104); CRITERION_NOT_FOUND lists existing ids (84-92); the safeParse failure path in luca_phase_write_verify enumerates per-issue path+message with control-char sanitization (luca-phase-write-verify.ts:53-71); plan-lint warnings carry file:line plus a concrete rewrite hint and an explicit "advisory — never blocking" summary (luca-plan-lint.ts:362-366). The deferred-regardless-of-met rule is enforced consistently in both consumers (verification-result.ts:149, validate-verification-ref.ts:93-95) and documented identically in the schema (schemas.ts:27-33).

CONSOLIDATED:
  MUST_FIX_COUNT: 2
  SHOULD_FIX_COUNT: 4
  NOTE_COUNT: 4
  CROSS_PHASE_COUNT: 2
