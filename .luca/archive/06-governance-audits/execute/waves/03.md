# Wave 3 — review fix cycle (enumeration completeness)

Split review verdict: simp+dx APPROVE; arch+sec REQUEST_CHANGES with 1 MUST-FIX — the audit's own "CLOSED enumeration" claim (REQ-08's core) had a governance gap.

## MUST-FIX — incomplete enumeration
`/lu` invokes `Skill(phase-execute)` at the execute step (skills/lu/index.ts:105), and the phase-execute skill honors relaxations NOT enumerated in the doc/const:
- `--skip-review` + `workflow.code_review: false` → skips code-review gate (phase-execute/index.ts:1234,:1270).
- `--skip-uat` + `workflow.uat_required: false` → skips UAT (phase-execute/index.ts:1609,:1611).
Both ADDED to RELAXATION_PATHS + doc table as SOFT floors with grep-verified sources.

## SHOULD-FIX — doc↔const drift
const was a strict subset of the doc. Added `--force-complex` (iteration-caps-force) and `/phase-plan --gaps` (gap-closure-research) to the const (were doc-only). Aligned the pr-validation `source` so doc + const both cite skills/gh-pr-address/index.ts.

## Completeness sweep (every skip/required/enabled token classified)
SOFT FLOORS (enumerated): --complexity=, --force-complex, --skip-memory, --skip-branch, --skip-research, --gaps, --skip-verify (standalone-only), --skip-review/code_review, --skip-uat/uat_required.
NON-FLOORS (excluded, rationale in confidence log): --skip-replay, --skip-verify-loop, --gaps-only, --quality-fixes (loop/re-entry controls — verify/review/checks still run); verification_tribunal_enabled, tribunal_enabled, root_cause_tribunal_enabled, stall_debate_enabled (COMPLEX+ quality enhancements, default on); tdd/testable (per-task exec mode). Flagged for reviewer confirmation via confidence log.

## Evidence
const new tokens grep ≥4 present (+doc); const floor count 17 (≥5); anti-01 (no CLI verb)=0, anti-02 (no Zod)=0; ac-02 doc gate-file cites=29 (≥8); pr-validation source agrees; tsc exit 0; rules clean; `luca checks run` passed. CLOSED conclusion re-asserted after the sweep.
