# Wave B — REQ-09 instruction trim + phantom-verb fix

## Task 2.B.1 (execute.ts + finalize.ts) — executor attestation
- Phantom fix: 6 phantom instructions → real verbs (`todo update --id <id> --status done --verification-criterion`; `luca retro` exit-code gate). Real verbs grep-confirmed against todo.ts/retro.ts.
- Trim: ALL-CAPS banner/CRITICAL/MANDATORY tone removed; load-bearing protocol survives (execute.ts 17 markers: checks run/rules run/claim-verify/atomic commit/branch guard/doctrine; finalize.ts 23: gap detection/postmortem gate/claim-verify/branch guard/gh pr create/verificationRef). ac-09 tsc exit 0.
- Follow-up: reworded finalize gotcha to drop literal phantom tokens (negation matched the grep) → ac-07 `grep -cE "todo move|move-batch|retro postmortem gate"` execute.ts+finalize.ts = 0; anti-05 = 0 across whole artifacts tree.

## Task 2.B.2 (review.ts) — executor attestation
- ac-10: CRITICAL CONSTRAINT 1→0 (strict decrease); Caveman "always active" line removed; IMMEDIATELY de-capped; duplicate Max-5 bullet dropped.
- anti-04 (survival): `ac-NN|deferred|verificationRef` 6→8 (≥ baseline — criteria grammar/deferred/verificationRef intact). audits/ path, max-5 rule, doctrine, review→execute loop-back retained. anti-06: agent-constraints.ts untouched. tsc exit 0.

## Task 2.B.3 (architect.ts + triage.ts) — executor attestation
- Density drop: architect CRITICAL 3→2, caps-banner 4→3, MANDATORY/NEVER/ALWAYS 2→1; triage CRITICAL 6→4, MANDATORY/IMMEDIATELY 3→0.
- Survival: architect ac-NN 12=12, anti-NN 5=5, plan_lint 3=3, canonical Verification Criteria/Deliverables template intact (only a Behavioral-Guidelines duplicate restatement removed); triage 5-row complexity table + level logic intact. anti-06 clean. tsc exit 0.

## Task 2.B.4 (verifier.ts + reviewer.ts) — executor attestation
- Density: verifier 8→4, reviewer 6→5. Survival: verifier VERIFICATION_DOCTRINE=2, deferred=3, probeType/evidence + verify.json schema intact, gotchas non-empty; reviewer 6 perspective slugs + MUST/SHOULD/NOTE severity + ≥3 threshold + output format intact, gotchas non-empty. anti-06 clean. tsc exit 0.

Wave close: `luca checks run` passed:true.
