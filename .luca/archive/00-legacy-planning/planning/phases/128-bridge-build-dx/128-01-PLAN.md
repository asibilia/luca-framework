---
phase: 128
plan: 128-01
title: Fix bridge CLI docs and deduplicate sanitizeJsonParse
complexity: SIMPLE
wave: 1
tdd: false
gap_closure: false
---

# Plan 128-01: Fix bridge CLI docs and deduplicate sanitizeJsonParse

## Objective

Fix two DX issues: (1) bridge CLI documentation lists 14 subcommands but there are 15, and (2) sanitizeJsonParse has 3 copies that should be consolidated.

## Tasks

### Task 1: Fix bridge CLI documentation (#45)

- Read `docs/architecture-overview.md` and find the bridge CLI subcommand list
- Read `packages/luca-framework/src/state/bridge.ts` to count actual subcommands
- Update docs to list all subcommands accurately

### Task 2: Deduplicate sanitizeJsonParse (#46)

The 3 known copies are linked with NOTE comments. Check:

- `packages/luca-framework/src/state/sanitize.ts`
- `packages/luca-framework/src/utils/sanitize.ts`
- `src/shared/__helpers/validation-utils.ts`

Consolidate into the canonical location (`src/shared/__helpers/validation-utils.ts`) and re-export from `src/shared/index.ts`. Update all import sites.

## Verification Criteria

- [ ] Bridge CLI docs match actual subcommand count
- [ ] sanitizeJsonParse exists in one canonical location
- [ ] All import sites updated
- [ ] `bunx --bun tsc --noEmit` passes
- [ ] `bun test` passes
