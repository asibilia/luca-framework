# 05-01: Dead Code & Duplicates

## Objective
Remove dead code, unused exports, and duplicate files. Quick wins with high impact.

## Tasks

### Task 1: Remove Unused Utility Functions
**File:** `src/shared/utils.ts`
- Remove `escapeMarkdown()` (lines 27-30) — zero references
- Remove `generateFileName()` (lines 32-34) — zero references
- Update any barrel file re-exports

**Verification:** `bun test __tests__/src/shared/utils.test.ts` — remove tests for deleted functions

### Task 2: Remove Unused Constants
**File:** `src/shared/constants.ts`
- Remove `CURSOR_DIR`, `AGENT_DIR`, `SKILL_DIR`, `RULE_DIR`, `LUCA_SUBDIR`
- Remove `SUPPORTED_FORMATS`
- Keep `FRAMEWORK_NAME` (used)
- If entire file becomes single constant, consider inlining

**Verification:** Grep for any remaining references before deletion

### Task 3: Consolidate Duplicate luca/ Files
**Files:**
- `src/agents/luca/lu-executor.agent.ts` → delete, use `src/agents/general/lu-executor.agent.ts`
- `src/agents/luca/lu-planner.agent.ts` → delete, use `src/agents/general/lu-planner.agent.ts`
- `src/rules/lu-workflow.rule.ts` → delete, use `src/rules/general/lu-workflow.rule.ts`

**Steps:**
1. Diff each pair to verify they're near-identical
2. Update all import references in `scripts/build-*.ts` and `scripts/compile-*.ts`
3. Delete the duplicate files
4. Run build scripts to verify

### Task 4: Clean Up Validation Module Structure
**Files:**
- `src/shared/validation/index.ts` just re-exports `validation-utils.ts`
- Consider: keep validation-utils.ts as public API surface (used by external consumers)
- Remove the unnecessary `validation/` directory if index.ts adds no value

**Verification:** `bun test` passes

## Exit Criteria
- `grep -r "escapeMarkdown\|generateFileName" src/` returns zero hits
- No duplicate agent/rule files in luca/ directories
- `bun test` passes
