---
phase: 129
plan: 129-01
title: XState guard and timeout fixes
complexity: MODERATE
wave: 1
tdd: false
gap_closure: false
---

# Plan 129-01: XState guard and timeout fixes

## Objective

Address the three XState-related items: verify #34 is already done, add idle timeout (#35), and enforce remaining complexity gating in guards (#44).

## Tasks

### Task 1: Verify shouldRunDiscussion guard (#34)

- The guard `shouldRunDiscussion` already exists in `packages/luca-framework/src/state/guards.ts`
- It's wired into `machine.ts` at the `ROUTE_COMPLETE` transition
- Tests exist in `__tests__/packages/luca-state/guards.test.ts`
- **This item is already complete.** Mark as done.

### Task 2: Add timeout/auto-transition to phase actor idle state (#35)

- Read `packages/luca-framework/src/state/machine.ts` to find idle states
- XState supports `after` for delayed transitions: `after: { 300000: { target: 'next_state' } }`
- Add an `after` timeout to the idle/waiting states so they auto-transition after a configurable delay
- The timeout should be configurable (default: 5 minutes / 300000ms)
- Add a `TIMEOUT` event type to the workflow events schema

### Task 3: Enforce complexity gating matrix in XState guards (#44)

- Read `packages/luca-framework/src/state/guards.ts` — already has shouldRunResearch, shouldRunDiscussion, shouldRunUAT
- Check the complexity gating matrix (`.claude/rules/complexity-gating.md`) for any missing guards
- Missing guards to potentially add: shouldRunCodeReview, shouldRunLearning
- Ensure guards read from the complexity matrix configuration, not hardcoded values
- Add tests for any new guards

## Verification Criteria

- [ ] #34 confirmed as already complete
- [ ] Idle states have timeout/auto-transition
- [ ] All complexity gating matrix steps have corresponding guards
- [ ] `bunx --bun tsc --noEmit` passes
- [ ] `bun test` passes (guard tests specifically)
