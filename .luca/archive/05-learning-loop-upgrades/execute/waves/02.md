# Round 2 — emitter wiring + 20-site content + smoke fixture

## Task A.3 (emit-agent.ts + emit-subagent.ts) — executor attestation
- ac-05: grep gotchas emit-agent.ts=1, emit-subagent.ts=1 (0 as-built); `gotchas: def.gotchas` added to renderBody({...}) at emit-agent.ts:60, emit-subagent.ts:68 (matches guidance/instructions pass pattern). tsc exit 0.

## Task A.4a (10 mode files) — executor attestation
- ac-06: `grep -rL "gotchas:" $(grep -rl "defineAgent(" modes/)` → 0 files; all 10 mode agents carry mode-specific gotchas[] (1-4 real footguns each). modes/index.ts untouched. tsc exit 0.

## Task A.4b (10 subagent files) — executor attestation
- ac-07: `grep -rL "gotchas:" $(grep -rl "defineSubagent(" subagents/)` → 0 files; all 10 subagents carry agent-specific gotchas[]. learner.ts: top-level gotchas field only, body untouched (`### TO_PERSIST` still = 1; gotcha reworded to avoid the literal token). subagents/index.ts untouched. tsc exit 0; rules clean.

## Task A.5 (compile-smoke.ts) — executor attestation
- ac-08.1: `bun compile-smoke.ts` → "SMOKE OK" exit 0 (new gotchas checks active+passing). ac-08.2: `grep -c "'## Gotchas'"` = 2 (the two golden checks; 0 as-built). gotchas:['<footgun>'] on both inline agent (~:76) + subagent (~:63) defs; golden checks mirror the `## Guidance` check at :268. tsc exit 0.

Round close: `luca checks run` passed:true; smoke fixture green.
