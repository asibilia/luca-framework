# Wave 01 — Phase 04: confidence-gating-docs-and-memory

## Tasks completed

### Task 1 — Gate-controller hardening (M1, M2, S1–S5)

**M1 (critical):** Moved the orphaned `finalize` step row from below the "Executor prompt injection" section back into the step table immediately after the `learn` row in `packages/luca-tools/src/artifacts/skills/lu/index.ts`. The table now reads contiguously: research → discuss → architect → plan → plan-review → execute → checks → verify → review → learn → **finalize**. No orphaned copy remains.

**M2:** Both the lu skill and command gate `ask` handlers now explicitly name the **AskUserQuestion** tool and include the directive "Block until the user answers — do NOT proceed on an unanswered question."

**S1 (idempotency):** Both skill and command gate sections now check for an existing `## Confidence Gate Resolutions` section in `plan-review.md` before appending, guarding against double-append on plan-review→plan→plan-review re-runs.

**S2 (resume):** The `plan-review` row in both skill and command now notes that a resuming orchestrator should check `plan-review.md` for an existing resolutions section before re-running the gate.

**S3 (all-auto):** Both skill and command now check `counts.research === 0 && counts.ask === 0` and proceed directly to execute when every bucket is auto — no stall.

**S4 (researcher template):** The `research`-bucket researcher spawn now has a concrete prompt template in both skill and command: passes entry's `decision`, `category`, `reasoning`, `alternatives`; asks for `RECOMMENDATION: <answer>` and `RATIONALE: <why>`.

**S5 (command parity):** The `/lu` command gate section (both the table row and the expanded Confidence Gate section) now includes `luca phase current` for resolving the phase dir and "use `Edit` not `Write`" instruction, matching the skill.

### Task 2 — Phase-2 cleanup carry-forwards

- `--resolution` invalid-value error message in `packages/luca-cli/src/commands/write-surface/confidence.ts` now cites `luca confidence log --help`.
- `lucaConfidenceLogTool.description` in `packages/luca-cli/src/write-surface/handlers/luca-confidence-log.ts` now lists `researchable?` and `resolution?` (was truncated at `reviewHint?`).
- Kept the manual `--resolution` enum check in confidence.ts — the z.enum handler validation produces a Zod error that is less friendly than the explicit CLI-level message; the manual check now cites --help, which is better UX.

### Task 3 — Decision doc + follow-up backlog

- Written: `docs/decisions/confidence-gated-lu.md`
- Covers: full-auto redefined; planning-time confidence signal (researchable/resolution); deterministic gate bucketing (auto/research/ask, medium→auto, fail-toward-ask, explicit resolution override); gate placement (tail of plan-review, persists to plan-review.md, injects to executor); 4 delivered phases; 2 follow-ups.
- Follow-ups captured via `luca todo add` (emits muninn_remember instructions):
  1. `luca confidence resolve` to mark journal entries resolved → true re-emit/re-gate loop
  2. Fix v13 `review → execute` transition gap

## Verification

- `bunx --bun tsc --noEmit`: PASS
- `bun run build`: PASS (agents:10 subagents:10 commands:17 skills:41 hooks:3 rules:0)
- Grep: exactly one `finalize` row in lu skill, contiguous with `learn` (line 92, following line 91 `learn`)
- Grep: `AskUserQuestion` present in both skill (line 121) and command (lines 61, 86)
- Grep: idempotency guard present in both skill and command
- Grep: all-auto check (`counts.research === 0 && counts.ask === 0`) present in both
- Grep: `luca phase current` + `` `Edit` not `Write` `` present in command

## Deviations

None. All M1/M2/S1–S5 items implemented exactly as specified in the plan and learn.md. The manual `--resolution` enum check in confidence.ts was retained (not removed) because it provides a friendlier CLI-level error with --help citation; noted in the wave.

## Files modified

- `packages/luca-tools/src/artifacts/skills/lu/index.ts` (M1, M2, S1, S2, S3, S4)
- `packages/luca-tools/src/artifacts/commands/lu.ts` (M2, S1, S2, S3, S4, S5)
- `packages/luca-cli/src/commands/write-surface/confidence.ts` (Task 2: --resolution error message)
- `packages/luca-cli/src/write-surface/handlers/luca-confidence-log.ts` (Task 2: description field)
- `docs/decisions/confidence-gated-lu.md` (created — Task 3)

No files under `packages/luca-core/` were modified.
