---
'@alecsibilia/luca-mastracode': patch
---

Add `/luca-init` slash command shim. The `luca-init` skill (added in v11.7.0) was previously not invocable via slash command despite its description claiming otherwise. This adds the missing `commands/luca-init.md` shim so users can activate the skill with `/luca-init`.
