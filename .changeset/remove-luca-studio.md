---
---

chore: remove the unused, unreleased internal `luca-studio` package (and its `tsconfig` exclude entry, root `css:studio`/`dev:studio` scripts, and a `luca-cli` comment reference).

No published-package impact: `luca-studio` is private and is not bundled into `@alecsibilia/luca`, so no version bump is needed.
