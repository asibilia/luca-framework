---
name: memory-audit
description: Paginated LLM-judged retro-pass over MuninnDB vault — classify trust tiers, write resumable cursor, emit per-run report
---

Activate the `memory-audit` skill. Optional arguments (`--dry-run` default, `--apply`, `--vault <name>`, `--resume`, `--limit <n>`):

$ARGUMENTS
