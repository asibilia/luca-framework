---
name: luca-telemetry-report
description: Aggregate `.planning/telemetry/*.jsonl` across recent runs — emit markdown report at `.planning/telemetry/report-<ISO>.md`. Read-only; never mutates state.
---

Activate the `luca-telemetry-report` skill. Optional arguments (`--runs <N>` default 10, `--since <ISO-date>`, `--vault <name>`):

$ARGUMENTS
