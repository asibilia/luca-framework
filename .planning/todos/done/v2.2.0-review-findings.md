---
title: Address v2.2.0 review findings (5 MEDIUM, 8 LOW)
area: pi-extensions
created: 2026-02-27T18:35:00-05:00
source: conversation
---

## Context

Five-expert review (architecture, security, performance, DX, simplification) of the v2.2.0 Pi Platform Maturity milestone. Full synthesis saved to `.planning/research/v2-2-0-review-synthesis.md`.

## Task

Address findings from the multi-expert code review. Prioritized below.

### 🟡 MEDIUM — Recommend addressing

| # | Finding | Domain | Fix |
|---|---------|--------|-----|
| M1 | `renderSubagents` + types (`SubagentEntry`, `SubagentDashState`) not exported from `__helpers/index.ts` barrel | Architecture | Add missing exports to `src/hooks/pi-extensions/__helpers/index.ts` |
| M2 | `luca-hooks.ts` exists in `.pi/extensions/` but not in `src/hooks/pi-extensions/` | Architecture | Add source file or document why it's deploy-only |
| M3 | `MAX_SUBAGENTS=8` limit only enforced in `luca-subagents.ts`, not in team/purpose-gating spawn paths | Security | Move limit check into `spawnPiSubprocess()` or the shared registry |
| M4 | No path traversal guard on session directory in `luca_subagent_continue` flow | Security | Add `isWithinDirectory(sessionDir, tmpdir())` check before spawning |
| M5 | Widget border-drawing code duplicated across 4 renderers in `widget-renderers.ts` | Simplification | Extract a `renderWidgetBox(title, width, contentFn)` helper |

### 🟢 LOW — Optional improvements

| # | Finding | Domain |
|---|---------|--------|
| L1 | `AgentFrontmatter` type exists in both Pi and core layers — needs clarifying doc comment | Architecture |
| L2 | `cleanupSessionDir()` uses `rmdirSync` instead of `rmSync({ recursive: true })` | Architecture |
| L3 | No subagent process timeout (stuck processes run indefinitely) | Security |
| L4 | Quick `line.startsWith("{")` check before `JSON.parse()` on stdout lines in `spawn.ts` | Performance |
| L5 | No usage example/doc for `subagent_continue` workflow | DX |
| L6 | `renderSubagents()` has no E2E widget test (unlike other renderers) | DX |
| L7 | `luca-hooks.ts` has 9 event handlers but no handler-level tests | DX |
| L8 | Two different mock styles (`Array` vs `Map`) across the two test files | Simplification |

## Notes

- All 240 tests pass, 0 failures
- Source/deploy sync verified clean (except M2)
- Security posture is strong overall: shell:false, sanitized inputs, 0o600 perms, stdin:ignore
- DRY extraction from Phase 67 was clean — no leaky abstractions
- Full review details: `.planning/research/v2-2-0-review-synthesis.md`
