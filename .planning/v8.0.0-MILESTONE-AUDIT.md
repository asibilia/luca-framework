# v8.0.0 Milestone Audit — Luca Studio MVP

**Date:** 2026-03-25
**Branch:** 103--v8-luca-studio-mvp
**Scope:** 244 files changed, +9,269 / -74 LOC, 8 phases, 19 todos

## Requirements Coverage

All 19 roadmap todos checked off: 19/19 complete.

## Integration Status

**Score:** 6/6 integration paths wired correctly
**Missing connections:** 3 (1 critical, 1 moderate, 1 low)

| #   | Gap                                                                                                                                                                       | Severity | Fix Location                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------- |
| 1   | `configAtom` never populated — no hook fetches GET /api/config to seed the draft layer. Pipeline save sends empty/stale payload.                                          | CRITICAL | app/pipeline/page.tsx or a provider       |
| 2   | Agent form field edits (description, enabled, model tier) stored in draft atom but `useAgentSave` only serializes `rawConfigText` — form edits silently discarded on save | MODERATE | hooks/use-agent-save.ts                   |
| 3   | Compile route (`POST /api/compile`) has no UI consumer — "Compiled" tab shows local placeholder                                                                           | LOW      | components/agents/agent-tab-container.tsx |

## Code Quality Findings

### CRITICAL (6 issues — must fix before shipping)

| #   | Issue                                                                                                                                                                         | Files                                                                                        |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| 1   | **Bun APIs in Next.js routes:** `Bun.file()`, `Bun.write()`, `Bun.CryptoHasher`, `Glob from "bun"` used inside Next.js API route handlers. These crash under Node.js runtime. | entity-route-helpers.ts, etag.ts, config-section-handler.ts, state/route.ts, ledger/route.ts |
| 2   | **shared-constant-registry.ts broken imports:** `~/agents/__helpers/` resolves to packages/luca-studio/agents/ (doesn't exist). File is dead code.                            | lib/shared-constant-registry.ts                                                              |
| 3   | **Sidecar \_\_helpers import:** Direct import from `src/compilers/__helpers/compile.ts` violates module-boundary Rule 4. Should use barrel.                                   | sidecar/compiler.ts                                                                          |

### HIGH (6 issues)

| #   | Issue                                                                                                | Source                            |
| --- | ---------------------------------------------------------------------------------------------------- | --------------------------------- |
| 1   | **Path traversal:** Entity `name` URL param not sanitized — `../../` can escape src/ directory       | security: entity-route-helpers.ts |
| 2   | **Arbitrary TS injection:** PUT body `rawConfigText` written to src/ .ts files without validation    | security: entity-route-helpers.ts |
| 3   | **Missing If-Match enforcement:** PUT proceeds without ETag header, bypassing concurrency guard      | security: entity-route-helpers.ts |
| 4   | **configDraftAtom circular set:** Self-referencing `set(configDraftAtom, value)` will stack overflow | dx: config-atoms.ts               |
| 5   | **Duplicate ETag implementation:** Inline in config/route.ts duplicates lib/etag.ts                  | arch: config/route.ts             |
| 6   | **Dynamic inline import:** `await import("node:fs/promises")` in ts-round-trip.ts                    | arch+dx: ts-round-trip.ts         |

### MEDIUM (7 issues)

| #   | Issue                                                                         |
| --- | ----------------------------------------------------------------------------- |
| 1   | Harness `command` field accepts arbitrary strings — potential shell injection |
| 2   | Shared `.tmp` race condition — concurrent writes use same temp file path      |
| 3   | `"use client"` on pipeline-atoms.ts (atoms don't need client boundary)        |
| 4   | KNOWN_ENTITY_TYPES duplicated between graph-types.ts and use-vault-health.ts  |
| 5   | Mixed camelCase/snake_case in config-section-schemas.ts                       |
| 6   | Unchecked LUCA_PROJECT_DIR env var — no .planning/ existence check            |
| 7   | Reflected user input in sidecar error messages                                |

### LOW (7 issues)

Missing JSDoc on some exports, lodash preference opportunities, deprecated export missing timeline, destructuring defaults in CodeMirrorWrapper props, unused atomFamily parameter naming.

## Security Summary

| Severity | Count | Key Concern                                                   |
| -------- | ----- | ------------------------------------------------------------- |
| HIGH     | 3     | Path traversal, TS injection, missing auth header             |
| MEDIUM   | 4     | Command injection, race condition, reflected input, env trust |
| LOW      | 3     | Response leakage, unvalidated data, dynamic filePath surface  |

## Tech Debt

1. **Bun/Node runtime mismatch:** 10+ files use Bun-only APIs in Next.js context. Needs systematic replacement with node:fs/promises or `bunx --bun next dev` enforcement.
2. **Dead code:** shared-constant-registry.ts, legacy components/workflow-editor/ directory.
3. **Missing barrels:** Inconsistent index.ts coverage across component subdirectories.
4. **Schema drift tooling:** `check:studio-drift` script referenced in docs but doesn't exist.
5. **configAtom hydration:** Server state atoms have no fetch-on-mount mechanism.

## Recommendations

### Must Fix (v8.0.1 audit closure)

1. Add name allowlist regex: `/^[a-z0-9][a-z0-9-]*$/` in entity route helpers
2. Add Zod schema for PUT body (rawConfigText + metadata)
3. Make If-Match header mandatory (428 on absence)
4. Replace Bun APIs with node:fs/promises in all Next.js route files
5. Fix configDraftAtom/routingDraftAtom circular set (two-atom pattern)
6. Add configAtom hydration hook in pipeline page or provider
7. Fix sidecar to use barrel import instead of \_\_helpers/ deep import
8. Delete shared-constant-registry.ts (dead code)

### Should Fix (v8.1.0)

- Constrain harness command field to z.enum() of safe commands
- Use random UUID suffix for .tmp files
- Connect compile route to agents "Compiled" tab
- Wire agent form edits through to save payload
