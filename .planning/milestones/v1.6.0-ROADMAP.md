# Archived Roadmap: v1.6.0 — Package & Publish

**Shipped:** 2026-02-16
**Theme:** Extract core components as standalone publishable packages, complete remaining memory improvements, and introduce multi-stack convention support.
**Stats:** 4 phases, 9 plans, 18 requirements, 1755 tests (1749 pass, 6 skip), 37 commits, 224 files changed (+10,874 / -703)

---

## Phase 40: XState Package Extraction

**Goal:** Extract the internal XState v5 state machine from `src/state-machine/` into a standalone `packages/luca-state/` package with its own package.json, CLI entry point, types, and comprehensive test suite — zero framework dependencies.
**Complexity:** COMPLEX | **Effort:** 8

**Plans:**

- [x] 40-01: Package scaffolding, core machine + types + guards extraction, shared utils extraction (Wave 1)
- [x] 40-02: Persistence layer, CLI entry point, event architecture (Wave 2)
- [x] 40-03: Snapshot generator, full test suite, package documentation (Wave 3)

**Outcome:** `packages/luca-state/` created with 347 tests, 88.8% coverage, 12-subcommand CLI, zero framework imports.

## Phase 41: Framework Integration Rewire

**Goal:** Update the internal framework to consume from `packages/luca-state/` instead of `src/state-machine/`, update all bridge CLI paths, maintain backward compatibility for skill/agent CLI calls.
**Complexity:** MODERATE | **Effort:** 5

**Plans:**

- [x] 41-01: Update monorepo workspace config, rewire all `src/` imports, update bridge CLI paths (Wave 1)
- [x] 41-02: Update all skill/agent shell commands referencing bridge, backward-compat shims, integration tests (Wave 2)

**Outcome:** 83 source files rewired, 327 generated files rebuilt, zero old-path references remaining, 2107 tests passing.

## Phase 42: Memory Suspend/Resume & Milestone Recall

**Goal:** Complete remaining Mastra-inspired memory improvements — suspend/resume with persistent state, auto-persist on context warnings, and milestone-scoped memory recall with temporal relevance scoring.
**Complexity:** MODERATE | **Effort:** 5

**Plans:**

- [x] 42-01: Suspend/resume types, checkpoint persistence, auto-persist on context warning, phase-execute suspend semantics (Wave 1)
- [x] 42-02: Milestone tags for MEMORY.md entries, milestone-scoped recall scoring, memory bridge updates, tests (Wave 2)

**Outcome:** SUSPEND/RESUME events added to state machine, checkpoint persistence in `.planning/checkpoints/`, auto-persist on context HIGH/CRITICAL, milestone-scoped recall with version distance scoring. 324 memory tests passing.

## Phase 43: Tech Stack Guideline Profiles

**Goal:** Organize code style rules under named tech stack profiles with config toggle and auto-detection integration, enabling multi-stack convention support.
**Complexity:** MODERATE | **Effort:** 4

**Plans:**

- [x] 43-01: Profile directory structure, config toggle, migrate TS-specific rules to typescript profile (Wave 1)
- [x] 43-02: Build system conditional includes, codebase-mapper integration, placeholder profiles for python/go/rust, tests (Wave 2)

**Outcome:** `src/rules/profiles/` with typescript, python, go, rust directories. 8 TS-specific rules migrated. Config toggle for opinionated_guidelines + tech_stack_profiles. Build produces 19 rules (11 general + 8 typescript). 40 profile-specific tests passing.

---

## Post-Milestone: Tech Debt Cleanup

After the audit passed (18/18 requirements), a tech debt cleanup was performed:

- Deleted `src/state-machine/` duplicate (~8,300 lines) — all consumers already migrated
- Replaced `BaseRuleImpl` abstract class with `createRule()` factory function
- Refactored all 19 rule files from class exports to functional instances
- Fixed local `Result<T>` in suspend-checkpoint.ts to import from shared/types

---

## Key Accomplishments

1. **Standalone XState Package** — `packages/luca-state/` with zero framework coupling, 347 tests, CLI entry point
2. **Complete Framework Rewire** — All 83 source files + 327 generated files updated, zero regressions
3. **Memory Suspend/Resume** — Checkpoint persistence, auto-persist on context warnings, milestone-scoped recall
4. **Multi-Stack Profiles** — Conditional rule loading with config toggle, auto-detection integration, placeholder profiles
5. **Tech Debt Cleanup** — Class-to-functional rule refactor, duplicate deletion, type deduplication
6. **No-Classes Compliance** — All 19 rule files now use functional `createRule()` pattern

---

_Archived: 2026-02-16_
