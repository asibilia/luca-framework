# v2.6.1 Milestone Audit

**Milestone:** v2.6.1 — Audit Gap Closure
**Date:** 2026-03-03
**Phases:** 2 (Phase 95 + Phase 96)
**Files Changed:** 95 TypeScript files

---

## Requirements Status

| Phase                                          | Goal                                                                                    | Status   |
| ---------------------------------------------- | --------------------------------------------------------------------------------------- | -------- |
| 95 — Tribunal Architecture & DRY Cleanup       | Resolve entity isolation violation, extract shared tribunal infrastructure, DRY cleanup | Complete |
| 96 — Convention Alignment & Security Hardening | Bun API migration, sanitizeForTemplate, lodash alignment, safeParse conversion          | Complete |

**Requirements: 9/9 complete (95-A through 95-E, 96-A through 96-D)**

---

## Integration Status

**Status: PASSED** (1 minor gap noted)

- Entity isolation: PASSED — No skills importing from agents, no cross-entity imports
- Barrel completeness: PASSED — src/shared/index.ts exports all tribunal infrastructure
- Import consistency: PASSED — All paths resolve, tsc passes
- Cross-phase wiring: PASSED — Files modified by both phases are internally consistent

**Minor gap:** `src/shared/__helpers/tribunal-detector.ts` imports `isDebateComplexity` from `~/complexity/__helpers/complexity-gate` — an undocumented T0-to-T0 peer `__helpers/` import. Low risk (both T0, no circular dependency), but should be documented or refactored. This is covered by the architecture findings below.

---

## Code Quality Findings

### HIGH (3 issues)

All three are the same pattern — **Rule 4 violation: direct `complexity/__helpers/` imports** instead of using the `~/complexity` barrel.

| File                                            | Line | Issue                                                                         |
| ----------------------------------------------- | ---- | ----------------------------------------------------------------------------- |
| `src/shared/__helpers/tribunal-detector.ts`     | 3    | `import { isDebateComplexity } from "~/complexity/__helpers/complexity-gate"` |
| `src/agents/__helpers/verification-tribunal.ts` | 1    | Same pattern                                                                  |
| `src/agents/__helpers/root-cause-tribunal.ts`   | 1    | Same pattern                                                                  |

**Fix:** Replace all three with `import { isDebateComplexity } from "~/complexity"` — the function is already exported from the complexity barrel.

### MEDIUM (8 issues)

| #   | File                           | Issue                                                                                 | Source   |
| --- | ------------------------------ | ------------------------------------------------------------------------------------- | -------- |
| 1   | `tribunal-consensus.ts:106`    | Native `.sort()` instead of lodash `orderBy`                                          | DX       |
| 2   | `hydration-snapshot.ts:308`    | `.parse()` instead of `.safeParse()` in `complexityToHydrationConfig`                 | DX       |
| 3   | `hydration-snapshot.ts:372`    | `.parse()` instead of `.safeParse()` in `generatePreFlightSnapshot`                   | DX       |
| 4   | `tribunal-rebuttals.ts:121`    | `source_agent`, `file` fields unsanitized in prompt interpolation                     | Security |
| 5   | `milestone-debate.ts:153`      | `milestoneVersion` unsanitized in prompt interpolation                                | Security |
| 6   | `milestone-debate.ts:274`      | Duplicated resolution-counting pattern (3rd copy of filter upheld/withdrawn/modified) | DRY      |
| 7   | `metrics-collector.ts:316`     | Repeated safeParse-or-throw pattern in switch (4 cases)                               | DRY      |
| 8   | `verification-tribunal.ts:137` | Three diagnostic prompt builders share ~80% identical structure                       | DRY      |

### LOW (8 issues)

| #   | File                             | Issue                                                               | Source       |
| --- | -------------------------------- | ------------------------------------------------------------------- | ------------ |
| 1   | `tribunal-detector.ts:1`         | Bare `"crypto"` instead of `"node:crypto"`                          | DX           |
| 2   | `convergence.ts:1`               | Bare `"crypto"` instead of `"node:crypto"`                          | DX           |
| 3   | `milestone-debate.ts:29`         | Direct `~/complexity/__schemas/` import when barrel suffices        | Architecture |
| 4   | `milestone-debate.schemas.ts:13` | Direct `~/complexity/__schemas/` import when barrel suffices        | Architecture |
| 5   | `agents/index.ts:54`             | Dual-export of tribunal symbols from both agents and shared barrels | Architecture |
| 6   | `tribunal-rebuttals.ts:200`      | Manual "group into Map" pattern duplicated in 4 files               | DRY          |
| 7   | `sanitize-template.ts:40`        | `${` removal leaves trailing `}` in output                          | Security     |
| 8   | `sanitize-template.ts:37`        | No defense against Unicode bidirectional control characters         | Security     |

---

## Gaps

**No functional gaps.** All 9 requirements (95-A through 96-D) are complete and verified.

**Convention gaps — PLANNED in v2.6.2 (Phase 97 + Phase 98):**

1. 3x `complexity/__helpers/` direct imports → should use barrel (HIGH) — **Phase 97-A**
2. 1x `.sort()` remaining in `tribunal-consensus.ts` (MEDIUM) — **Phase 97-B**
3. 2x `.parse()` remaining in `hydration-snapshot.ts` (MEDIUM, out of 96-D scope) — **Phase 97-B**
4. 2x unsanitized prompt interpolation sites (MEDIUM) — **Phase 97-B**

---

## Tech Debt — PLANNED in v2.6.2

| Item                                                                                         | Severity | Source       | Planned    |
| -------------------------------------------------------------------------------------------- | -------- | ------------ | ---------- |
| Resolution-counting pattern duplicated 3x (rebuttals, buildTribunalResult, milestone-debate) | MEDIUM   | DRY          | Phase 98-A |
| Diagnostic prompt builders share ~80% structure, could use factory                           | MEDIUM   | DRY          | Phase 98-B |
| Repeated safeParse-or-throw pattern in metrics-collector switch (4 cases)                    | MEDIUM   | DRY          | Phase 98-C |
| Manual Map-grouping idiom duplicated in 4 files (could use lodash groupBy)                   | LOW      | DRY          | Phase 98-D |
| Dual-export of tribunal symbols from both agents and shared barrels                          | LOW      | Architecture | Phase 97-A |
| Bare `"crypto"` imports (2 files) should use `"node:crypto"` prefix                          | LOW      | DX           | Phase 97-B |
| sanitizeForTemplate edge cases (trailing `}`, Unicode bidi)                                  | LOW      | Security     | Phase 97-B |
| sanitizeForTemplate intentionally duplicated T0/T3 (documented, acceptable)                  | LOW      | DRY          | No action  |

---

## Harness Confirmation

- **TypeCheck:** PASSED (0 errors)
- **Tests:** 3146 pass / 3 fail (pre-existing module resolution issue, documented in CLAUDE.md)
- **Debate:** Skipped (TRIVIAL complexity, below COMPLEX gate)
