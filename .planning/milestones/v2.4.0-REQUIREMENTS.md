# Requirements Archive — v2.4.0 Pi Platform Completion

## Overview

Activate runtime model routing via `pi.setModel()`, add interactive dialogs and keyboard shortcuts, and improve tool resilience with AbortSignal support and session lifecycle handling.

## Requirements

### R1: Runtime Model Routing

**Priority:** HIGH | **Status:** Complete

- R1.1: `model-routing.ts` helper module created with tier resolution logic
- R1.2: `pi.setModel()` called in role assignment based on agent's `modelTier`
- R1.3: Model-aware agent spawning passes tier to subagents
- R1.4: Context introspection enables adaptive behavior based on available context

### R2: Interactive Dialogs & Shortcuts

**Priority:** HIGH | **Status:** Complete

- R2.1: `dialogs.ts` helper module for interactive user prompts
- R2.2: `/switch-model` command allows runtime model tier changes
- R2.3: `/set-complexity` command for manual complexity override
- R2.4: `/config` command for runtime config inspection
- R2.5: Keyboard shortcuts registered for common workflow actions

### R3: Tool Resilience & Session Lifecycle

**Priority:** HIGH | **Status:** Complete

- R3.1: AbortSignal support in `luca_verify` tool for cancellation
- R3.2: AbortSignal support in `luca_tilldone` tool for cancellation
- R3.3: AbortSignal support in subagent spawn operations
- R3.4: `session_compact` event handler for context compaction
- R3.5: `session_shutdown` event handler for graceful cleanup
- R3.6: Structured `details` field in tool return values
- R3.7: Custom message renderer for rich tool output formatting

### R4: Agent Role Content Refresh

**Priority:** MEDIUM | **Status:** Complete

- R4.1: All agent roles updated from old project references to Luca framework context
- R4.2: Role content refreshed across all 3 platforms (.claude/, .cursor/, .pi/)

---

_Archived: 2026-03-01_
