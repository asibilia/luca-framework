# v8.5.1 — Audit Gap Closure

**Status:** Complete
**Date:** 2026-03-28
**Stats:** 2 phases, 3 plans, 13 commits, 78 files changed (+8,704/-10,488 LOC)

## Summary

Closed 11 code quality and security findings from the v8.5.0 milestone audit. Extracted shared factories for enforcement hooks and context schemas, then hardened with input validation, Zod parsing, Bun.file migration, and file permission controls.

## Phases

### Phase 225: DRY Consolidation (MODERATE)

**Goal:** Extract shared factories to eliminate ~650 LOC of duplication across 5 decomposed skills.

- [x] dry-hook-factory — `createSubSkillEnforcementHook(config)` factory replacing 5 identical hooks (DRY-001)
- [x] dry-context-factory — `createContextHelpers<T>(path, schema)` factory replacing 5 identical read/write helpers (DRY-002)
- [x] dry-shared-constants — `ABORT_TRANSITION` shared module, standardized orchestrator patterns (DRY-003, DRY-004, DRY-005)

**Result:** 3 new shared modules, 14 consumer files refactored, ~1,004 LOC duplication eliminated.

### Phase 226: Security Hardening (SIMPLE)

**Goal:** Harden enforcement hooks with input validation, schema-based parsing, and file permission controls.

- [x] sec-skill-matching — Exact match pattern replacing substring `includes()` (SEC-001)
- [x] sec-context-validation — Zod safeParse + Bun.file migration for context files (SEC-002, SEC-006)
- [x] sec-file-permissions — 0o600 on context files and dedup guard files (SEC-003, SEC-004, SEC-005)

**Result:** All 6 security findings resolved. pr-address hook migrated to factory (bonus).

## Key Deliverables

1. **Enforcement Hook Factory** (`src/hooks/__helpers/enforcement-hook-factory.ts`) — Config-driven factory with fail-closed default and initialSkill fail-open exception
2. **Context Helpers Factory** (`src/skills/__schemas/context-helpers.ts`) — Generic `createContextHelpers<T>(path, schema)` with type-safe writes (no escape hatch)
3. **Shared Transitions** (`src/skills/__schemas/states/shared-transitions.ts`) — Single ABORT_TRANSITION constant for all 5 state machines
4. **Security Hardening** — Exact skill matching, Zod validation, Bun.file I/O, 0o600 file permissions
5. **Complete Factory Migration** — All 5 enforcement hooks now use the factory (including pr-address)
