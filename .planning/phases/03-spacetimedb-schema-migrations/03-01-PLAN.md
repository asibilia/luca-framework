# Phase 3 Plan 01: SpacetimeDB Schema Verification & Index Enhancement

## Objective

Verify that prior work has addressed the 4 schema migration todos (#65, #48, #42, #43) and make the remaining small improvements (additional indexes, cleanup config alignment).

## Context

Codebase analysis shows most Phase 3 work was completed in prior milestones:

- **#65**: Schema already uses `brainMd`/`memoryMd`/`workingMd`/`proceduresMd`
- **#48**: Singleton uniqueness enforced by PK design (`id: t.u64().primaryKey()`)
- **#43**: Sequence numbers computed atomically server-side in `append_ledger_entry`
- **#42**: TTL cleanup scheduler exists; missing `eventType`/`timestamp` indexes

## Wave 1: Add Missing Indexes

### Task 1.1: Add eventType index to observer_events

**File:** `packages/luca-spacetime/spacetimedb/src/schema.ts`

Add a btree index on `eventType` to the `ObserverEvents` table definition for filtering by event type.

**Verification:** `bunx --bun tsc --noEmit` passes. Index definition follows the `{tableName}_{columnName}` naming convention per SpacetimeDB CLAUDE.md.

### Task 1.2: Add timestamp index to observer_events

**File:** `packages/luca-spacetime/spacetimedb/src/schema.ts`

Add a btree index on `timestamp` to the `ObserverEvents` table for time-range queries.

**Verification:** `bunx --bun tsc --noEmit` passes.

### Task 1.3: Add eventType index to observer_events in index.ts queries (if needed)

Check if any queries in the codebase filter observer_events by eventType or timestamp and could benefit from using the new indexes. Update query patterns if applicable.

**Verification:** Grep for `observer_events` SQL queries in the codebase. No broken references.

## Wave 2: Verify Completed Work & Update Roadmap

### Task 2.1: Verify #65 field rename is complete

Confirm no SpacetimeDB payload construction still uses `brainJson`/`memoryJson` etc. as field keys sent to reducers. Local variable names (`const brainJson = ...`) are acceptable.

**Files to check:**

- `src/memory/__helpers/bridge.ts` — verify `callReducer("update_memory_files", {...})` payload uses `brainMd` keys
- Observer hooks that consume MemoryFiles table data

**Verification:** Grep for `brainJson.*callReducer|brainJson.*reducer|update_memory_files.*brainJson` returns no matches.

### Task 2.2: Verify #48 singleton constraints are sound

Confirm all 7 singleton reducers use the `SINGLETON_ID = 1n` + find/update/insert pattern.

**Tables to verify:** WorkflowState, HarnessResults, SessionPlans, TribunalResults, MemoryFiles, Metrics, WorkflowConfig

**Verification:** Each reducer follows the `const existing = ctx.db.X.id.find(SINGLETON_ID)` pattern.

### Task 2.3: Verify #43 sequence race fix

Confirm `append_ledger_entry` reducer computes sequence numbers atomically via `ctx.db.ledgerEntries` iteration inside the reducer.

**Verification:** The reducer in `packages/luca-spacetime/spacetimedb/src/index.ts` uses transactional sequence computation.

### Task 2.4: Update ROADMAP.md

Mark all 4 Phase 3 plans as complete (`[x]`).

**Verification:** ROADMAP.md shows Phase 3 fully complete.

## Success Criteria

- [ ] `eventType` and `timestamp` indexes added to observer_events
- [ ] All 4 todos verified as addressed
- [ ] `bunx --bun tsc --noEmit` passes
- [ ] ROADMAP.md Phase 3 plans marked complete
