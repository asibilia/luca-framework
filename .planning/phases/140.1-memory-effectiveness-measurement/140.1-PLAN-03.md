---
phase: 140.1
plan: 3
type: feature
autonomous: true
wave: 3
depends_on: [2]
---

# Phase 140.1 Plan 3: Surfacing — Progress Skill Memory Health and Milestone-Complete Pruning

## Objective

Surface memory effectiveness data to the developer. Add a memory health section to the progress skill so developers can see recall precision, hit rate, and stale memory counts at a glance. Add stale memory detection and consolidation to milestone-complete so memory stays clean across milestones.

> Appetite: Default (Medium baseline, not explicitly declared)

## Context

- @src/skills/general/progress.skill.ts (Step 5: Present Rich Status Report, lines 79-141)
- @src/skills/general/milestone-complete.skill.ts (Learning Consolidation section, lines 40-82)
- @src/shared/\_\_schemas/memory-metrics.schemas.ts (MemoryHealthSummarySchema from Plan 1)
- @.planning/phases/140.1-memory-effectiveness-measurement/140.1-RESEARCH.md (integration points 8-9)

## Tasks

### 1. Add memory health section to progress skill

**Type:** auto
**TDD:** false
**Depends on:** none

In `src/skills/general/progress.skill.ts`, add a new step between the existing Key Decisions and Blockers/Concerns sections within Step 5 (Present Rich Status Report).

Add a subsection that:

1. **Recalls recent memory metrics** from MuninnDB:

```
### Memory Health (if memory feedback data exists)

Recall memory effectiveness metrics from MuninnDB:

mcp__muninn__muninn_recall(
  vault: "default",
  context: "metric:memory-recall-precision metric:memory-hit-rate current milestone",
  mode: "recent",
  limit: 10
)

If no memory metrics found, skip this section entirely (do not display empty sections).

If metrics found, display:
```

2. **Renders a memory health banner:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 Memory Health
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Recall Precision : {recall_precision}% ({applied}/{recalled} engrams applied)
  Hit Rate         : {hit_rate}% (phases where memory was useful)
  Token Cost       : {memory_tokens_injected} tokens across {phase_count} phases
  Stale Engrams    : {stale_count} engrams with no positive feedback in 5+ phases

  Status: {healthy|degraded|no_data}
  {If degraded: "Consider running /milestone-complete to prune stale memories"}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

3. **Computes health status:**
   - `healthy`: precision >= 0.5 AND stale_count <= 3
   - `degraded`: precision < 0.5 OR stale_count > 3
   - `no_data`: no memory metrics found for current milestone

The precision and hit rate values are extracted from the most recent `metric:memory-recall-precision-*` and `metric:memory-hit-rate-*` engrams. Average across all phases in the current milestone.

**Files to create/edit:**

- `src/skills/general/progress.skill.ts` (MODIFY — Step 5 status report)

**Verification:**

- Memory health section displays after Key Decisions
- Conditional rendering (no empty sections when no data)
- Health status logic documented (healthy/degraded/no_data)
- Banner format consistent with existing progress banners
- `bunx --bun tsc --noEmit` passes

### 2. Add stale memory pruning to milestone-complete

**Type:** auto
**TDD:** false
**Depends on:** none

In `src/skills/general/milestone-complete.skill.ts`, add a new Step 0.5 between the existing Step 0 (Final Learning Extraction) and Step 1 (Archive Milestone Memory). This step handles stale memory detection, pruning, and consolidation.

Add the following step:

```
### Step 0.5: Stale Memory Detection and Pruning

Before archiving, analyze memory health and prune stale engrams.

**1. Recall all memory metrics for this milestone:**

mcp__muninn__muninn_recall(
  vault: "default",
  context: "metric:memory-recall-precision metric:memory-hit-rate {milestone_version}",
  mode: "deep",
  limit: 50
)

**2. Identify stale engrams:**

Recall all pattern/decision/pitfall engrams and cross-reference with feedback data:

mcp__muninn__muninn_recall(
  vault: "default",
  context: "pattern: decision: pitfall: for {milestone_version}",
  mode: "deep",
  limit: 100
)

An engram is "stale" if:
- It was recalled 3+ times across the milestone (appeared in recalledEngrams)
- It received feedback with useful=false more than useful=true
- OR it was never recalled at all during the entire milestone (zero appearances)

**3. Prune stale engrams:**

For each stale engram, decide:
- **Forget** (if never recalled and older than 2 milestones): `mcp__muninn__muninn_forget(vault: "default", id: engram_id)`
- **Evolve** (if recalled but low usefulness): `mcp__muninn__muninn_evolve(vault: "default", id: engram_id, new_content: "...", reason: "Low usefulness across milestone {version}: {useful_count}/{total_feedback} positive feedback")`
- **Consolidate** (if multiple similar low-utility engrams): `mcp__muninn__muninn_consolidate(vault: "default", ids: [id1, id2, ...], merged_content: "...")`

**4. Report pruning results:**

Log pruning summary:
"Memory pruning: {forgotten} engrams forgotten, {evolved} engrams evolved, {consolidated} engrams consolidated from {total_stale} stale candidates."

Store pruning report as a milestone metric:
mcp__muninn__muninn_remember(
  vault: "default",
  concept: "metric:memory-pruning-{milestone_version}",
  content: JSON.stringify({
    forgotten: {count},
    evolved: {count},
    consolidated: {count},
    total_stale: {count},
    total_engrams_analyzed: {count},
    pruned_at: new Date().toISOString()
  })
)
```

Also update the existing Step 1 (Archive Milestone Memory) to include memory effectiveness metrics in the archive:

```
Include in archive:
- All patterns validated during this milestone
- Key decisions and their outcomes
- Pitfalls discovered and avoided
- Memory effectiveness summary (precision, hit rate, token cost across milestone)
```

**Files to create/edit:**

- `src/skills/general/milestone-complete.skill.ts` (MODIFY — insert Step 0.5, update Step 1)

**Verification:**

- Step 0.5 appears between Step 0 and Step 1
- Stale detection logic is clear (3 criteria)
- Pruning actions documented (forget, evolve, consolidate)
- Pruning metrics stored as MuninnDB engram
- Archive includes memory effectiveness summary
- `bunx --bun tsc --noEmit` passes

## Verification

After all 2 tasks:

1. `bunx --bun tsc --noEmit` passes with zero errors
2. Progress skill shows memory health when data exists, hides when no data
3. Milestone-complete prunes stale memories before archiving
4. Both skills follow existing banner/formatting patterns

## Success Criteria

- Developer can answer "is memory helping?" by running `/progress`
- Memory health banner shows precision, hit rate, token cost, stale count
- Stale memory detection runs at milestone boundaries
- Pruning actions (forget, evolve, consolidate) documented and instructed
- Pruning results stored as milestone metric engram

## Output Specification

- `src/skills/general/progress.skill.ts` (MODIFIED — memory health section in Step 5)
- `src/skills/general/milestone-complete.skill.ts` (MODIFIED — Step 0.5 pruning + Step 1 archive update)
