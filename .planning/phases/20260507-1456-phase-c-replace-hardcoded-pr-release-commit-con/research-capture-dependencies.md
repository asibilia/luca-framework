# Research Capture — Ecosystem Dependencies

**Subagent**: researcher
**Perspective**: dependencies
**Timestamp**: 2026-05-07T18:56Z

## Findings

Subagent ran reads of tool-manifest, schema, memory, executor; output truncated. Key findings cross-referenced from risk + patterns dimensions:

### Tool-manifest gap matrix
| Mode | `projectPreferences` actions | Notes |
|---|---|---|
| triage | consult, consult-section, seed | Sentinel Step 1.6 |
| research | consult, consult-section | |
| architect | consult, consult-section | Phase B uses |
| execute | consult, consult-section | Phase C will use for pre-commit |
| review | consult, consult-section | |
| finalize | consult, consult-section | Phase C will use for PR title + bump |
| build | consult, consult-section | |
| fast | consult, consult-section | |
| discuss | consult, consult-section | |
| **plan** | **MISSING** | **RISK-2: alwaysApply rule fires here, fails** |

### Schema vs memory divergences (RISK-1, CRITICAL)
| Field | Schema (`state/project-preferences.ts`) | MuninnDB memory |
|---|---|---|
| PR title | `pr.titleFormat` | `pr.titleTemplate` |
| Bump map | `release.versionBump` | `release.bumpMapping` |
| Commit convention | enum `'conventional'\|'none'` | string `"conventional-commits"` |
| Tracker kind | enum `'github'` | string `"github-issues"` |
| Schema version | `schemaVersion` | `version` |
| PR forbidden | NOT IN SCHEMA | `pr.forbidden[]` |
| PR titleExamples | NOT IN SCHEMA | `pr.titleExamples[]` |
| Commit trailers | NOT IN SCHEMA | `commits.trailers` |
| subjectMaxLength | NOT IN SCHEMA | `commits.subjectMaxLength: 72` |

### Persistence layer (RISK-3)
- `consult-section` reads `.planning/preferences.json` (file), NOT MuninnDB memory.
- `loadProjectPreferences()` in `state/project-preferences.ts` does file → Zod.parse (silently dropping unknown fields, coercing invalid enums to defaults).
- Memory is the canonical/restore source; file is the runtime source.
- `.planning/preferences.json` must be committed (or generated by CI) — verify.

### gh-prepare invocation chain
- `gh-prepare` is a skill — invoked from finalize mode (and possibly directly via `/gh-prepare`).
- All invoking modes have `projectPreferences.consult-section` in scope. ✓
- changeset detection: skill reads `.changeset/config.json` for fixed-group info (auto-detected, not in preferences). Bump-MAP per commit type IS in preferences (`release.versionBump`).
- `gh pr create --title` is where the templated title gets passed.

### Backward compat
- Existing repos w/o seeded preferences: hit auto-init on triage Step 1.6.
- `luca-init` skill respects `state.oversight === 'full-auto'` — auto-seeds without `ask_user` (per SKILL.md:53-57). Headless-safe.
- For non-triage modes (gh-prepare invoked standalone), preferences may be unseeded → consult returns null → must handle gracefully.

### alwaysApply blast radius
- `pr-title-format.md` injected into EVERY mode of EVERY consumer. New body must:
  - Work in modes WITHOUT `projectPreferences` tool (graceful degradation, not runtime error)
  - Not instruct non-triage modes to invoke luca-init (they cannot)
