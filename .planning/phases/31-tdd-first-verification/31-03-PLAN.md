---
id: 31-03
title: "Verifier signal priority update + fallback rules"
phase: 31
wave: 3
depends_on: [31-02]
requirements: [TDD-05, TDD-06]
tdd: false
must_haves:
  truths:
    - "lu-verifier uses test pass/fail as primary T1 signal when tests exist"
    - "lu-verifier uses goal-backward analysis as secondary T3 signal"
    - "lu-verifier falls back to T3 (goal-backward) as primary when no tests exist"
    - "Executor and verifier recognize testable: false flag and skip TDD cycle"
    - "Non-testable work types (docs, config, research) are explicitly documented as TDD-exempt"
  artifacts:
    - path: "src/agents/general/lu-verifier.agent.ts"
      provides: "Explicit T1/T3 signal naming, test results as primary verification signal, goal-backward as secondary"
    - path: "src/agents/luca/lu-executor.agent.ts"
      provides: "testable: false frontmatter support in TDD execution flow"
    - path: "src/agents/general/lu-test-writer.agent.ts"
      provides: "Non-testable detection section already covers auto-detection (from Plan 31-01)"
  key_links:
    - from: "src/agents/general/lu-verifier.agent.ts"
      to: "src/harness/runner.ts"
      via: "Step 6.5 harness results consumption with T1 priority designation"
    - from: "src/agents/general/lu-verifier.agent.ts"
      to: "src/agents/general/lu-verifier.agent.ts"
      via: "Step 9.5 goal-backward check with T3 secondary designation"
---

# Plan 31-03: Verifier Signal Priority Update + Fallback Rules

## Objective

Update `lu-verifier.agent.ts` to explicitly name its verification signals as T1 (primary: test pass/fail from harness) and T3 (secondary: goal-backward objective check), making the signal hierarchy clear and deterministic. When test results exist, they are the primary verification signal because they are fast, deterministic, and directly encode the plan's verification criteria. Goal-backward analysis provides deeper semantic verification as a secondary check. When no tests exist (non-testable work), goal-backward becomes the primary signal. Additionally, add `testable: false` frontmatter support to the executor and document the fallback rules for non-testable work types.

## Context

@src/agents/general/lu-verifier.agent.ts -- The verifier agent. Currently has Step 6.5 (Incorporate Harness Results) and Step 9.5 (Goal-Backward Objective Check) but does not explicitly name these as T1/T3 signals or establish priority between them. The harness results section treats test pass/fail as informational, not as the primary verification signal. The goal-backward check does not reference its relationship to harness signals.
@src/agents/luca/lu-executor.agent.ts -- The executor agent, modified in Plan 31-02 to include TDD execution flow. The `tdd_execution_flow` section needs to handle `testable: false` frontmatter to skip TDD for non-testable tasks.
@src/agents/general/lu-test-writer.agent.ts -- The test writer agent from Plan 31-01. Its `non_testable_detection` section already defines criteria for detecting non-testable work. This plan ensures the executor and verifier honor those signals.
@src/harness/runner.ts -- The harness runner. Returns `HarnessResult { status, checks[], totalErrors, totalWarnings, duration }`. The verifier reads this in Step 6.5.
@src/harness/parsers/bun-test.ts -- The bun test parser. Parses `bun test` output into structured results. The verifier consumes these as T1 signals.
@.planning/phases/31-tdd-first-verification/RESEARCH.md -- Research findings establishing T1/T3 signal naming convention and fallback rules.

## Tasks

### Task 1: Add explicit T1/T3 signal naming to lu-verifier

**Goal:** Modify `src/agents/general/lu-verifier.agent.ts` to explicitly designate Step 6.5 (harness results) as the T1 primary verification signal and Step 9.5 (goal-backward) as the T3 secondary signal. Update the verification process to use test results as the primary determinant of verification status when tests exist.

**Files:** `src/agents/general/lu-verifier.agent.ts` (modify)

**Steps:**

1. Update Step 6.5 ("Incorporate Harness Results") to add T1 signal designation. Add the following content at the beginning of the Step 6.5 section:

   ```
   ## Step 6.5: Incorporate Harness Results — T1 Signal (PRIMARY)

   **Signal Designation:** T1 — Primary Verification Signal

   Test pass/fail results from the verification harness are the PRIMARY verification signal because:
   - **Deterministic**: Same input always produces same result
   - **Fast**: Runs in seconds, not minutes
   - **Specification-encoded**: Tests directly encode plan verification criteria (when TDD is used)
   - **Mechanical**: No LLM judgment required for pass/fail determination

   When TDD-generated tests exist and pass, this is strong evidence that the plan's verification criteria are met. The verifier should weight this signal heavily in its overall status determination.
   ```

2. Update the harness result routing in Step 6.5 to be more directive about T1 signal weight:

   **If harness status = "passed" AND TDD tests included:**
   - T1 signal: STRONG PASS
   - Add to report: "T1 Signal (PRIMARY): All automated checks passed including TDD-generated tests. Plan verification criteria mechanically confirmed."
   - Verification status can be `passed` even without full T3 analysis if T1 is strong and no anti-patterns found

   **If harness status = "passed" but NO TDD tests:**
   - T1 signal: PARTIAL (mechanical checks pass but no specification-encoded tests)
   - T3 signal becomes co-primary (goal-backward analysis needed to fill the gap)

   **If harness status = "failed_after_fixes":**
   - T1 signal: FAIL (remaining errors are blockers)
   - Map each remaining error to truths/artifacts it affects
   - These mechanical failures MUST appear in the gaps section regardless of T3 results

3. Update Step 9.5 ("Goal-Backward Objective Check") to add T3 signal designation. Add the following content at the beginning of Step 9.5:

   ```
   ## Step 9.5: Goal-Backward Objective Check — T3 Signal (SECONDARY)

   **Signal Designation:** T3 — Secondary Verification Signal

   Goal-backward analysis is the SECONDARY verification signal because:
   - **Semantic**: Checks intent and purpose, not just mechanics
   - **Thorough**: Catches specification gaps that tests miss
   - **Subjective**: Requires LLM judgment (less deterministic than T1)
   - **Complementary**: Fills gaps that mechanical testing cannot cover

   When T1 (test results) is a STRONG PASS, T3 provides additional confidence but should NOT override T1. When T1 is PARTIAL (no TDD tests) or absent, T3 becomes the PRIMARY signal.
   ```

4. Update Step 9 ("Determine Overall Status") to incorporate T1/T3 signal priority:

   Add a signal priority matrix:

   ```
   ### Signal Priority Matrix

   | T1 (Harness/Tests) | T3 (Goal-Backward) | Overall Status |
   |---------------------|---------------------|----------------|
   | STRONG PASS         | PASS                | passed         |
   | STRONG PASS         | PARTIAL             | passed (T1 overrides — tests confirm spec) |
   | STRONG PASS         | FAIL                | human_needed (conflict — needs review) |
   | PARTIAL             | PASS                | passed         |
   | PARTIAL             | PARTIAL             | human_needed   |
   | PARTIAL             | FAIL                | gaps_found     |
   | FAIL                | any                 | gaps_found (T1 failure is always blocking) |
   | absent              | PASS                | passed         |
   | absent              | PARTIAL             | human_needed   |
   | absent              | FAIL                | gaps_found     |
   ```

   Key rule: **T1 FAIL always results in gaps_found**, regardless of T3 status. Mechanical failures are non-negotiable.

   Key rule: **T1 STRONG PASS with T3 PARTIAL still passes** because TDD tests directly encode the specification. If the specification tests pass, partial semantic analysis does not block.

   Key rule: **T1 STRONG PASS with T3 FAIL triggers human_needed** because the conflict between mechanical passing and semantic failure needs human judgment.

**Verification:**

- [ ] Step 6.5 has T1 signal designation with rationale
- [ ] Step 6.5 distinguishes between TDD tests present vs absent in harness results
- [ ] Step 9.5 has T3 signal designation with rationale
- [ ] Step 9 includes signal priority matrix
- [ ] T1 FAIL always results in `gaps_found`
- [ ] T1 STRONG PASS with T3 PARTIAL results in `passed`
- [ ] `bunx --bun tsc --noEmit` passes

---

### Task 2: Add testable: false frontmatter support

**Goal:** Update the lu-executor's TDD execution flow to check for `testable: false` in plan or task frontmatter and skip the TDD cycle for those items. This enables non-testable work (documentation, configuration, research) to proceed without being blocked by the TDD requirement.

**Files:** `src/agents/luca/lu-executor.agent.ts` (modify)

**Steps:**

1. In the `tdd_execution_flow` section (added in Plan 31-02), add a pre-check before Step TDD-1:

   ````
   **Step TDD-0: Check Testability**

   Before entering the TDD cycle, check if the task or plan is marked as non-testable:

   1. Check plan frontmatter for `testable: false`:

      ```bash
      grep -q "testable: false" {plan_path} && echo "NON_TESTABLE" || echo "TESTABLE"
   ````

   2. Check if lu-test-writer returned `testable: false` for this task (from its non_testable_detection output).

   3. Auto-detect non-testable work by task type:
      - Documentation-only tasks (only creates/modifies .md files): NON_TESTABLE
      - Configuration changes (only modifies .json, .toml, .yaml config files): NON_TESTABLE
      - Research tasks (type="research" in plan): NON_TESTABLE
      - Planning updates (only modifies .planning/ directory): NON_TESTABLE

   **If NON_TESTABLE:**

   Skip TDD-1 through TDD-4. Execute the task normally using standard execution flow.

   Log:

   ```bash
   echo "- $(date -u +%H:%M) [TDD-SKIP] Task '{task_name}' is non-testable: {reason}. Using standard execution." >> .planning/WORKING.md
   ```

   The verifier will use goal-backward (T3) as the primary signal for this task instead of test results (T1).

   **If TESTABLE:** Proceed to Step TDD-1 (spawn lu-test-writer).

   ```

   ```

2. In the `execute_tasks` section, add the testable check alongside the tdd check:

   ```
   - Check if task has `testable="false"` attribute -> skip TDD even if plan has tdd: true
   ```

**Verification:**

- [ ] `tdd_execution_flow` section has Step TDD-0 (testability check) before Step TDD-1
- [ ] Auto-detection covers: .md only, config only, research type, planning directory
- [ ] Non-testable tasks are logged to WORKING.md with reason
- [ ] `testable: false` in frontmatter skips TDD cycle
- [ ] `bunx --bun tsc --noEmit` passes

---

### Task 3: Document fallback rules in verifier agent

**Goal:** Add a dedicated section to the lu-verifier agent that documents the fallback rules for non-testable work. When no TDD tests exist for a task (because it is non-testable), the verifier must know to use goal-backward (T3) as the primary signal and what standards to apply.

**Files:** `src/agents/general/lu-verifier.agent.ts` (modify)

**Steps:**

1. Add a new subsection within the `role` section content (the verifier uses a single large section titled `"role"` containing all steps), positioned between Step 6.5 and Step 7. Title it "Step 6.7: Non-Testable Work Fallback":

   ````
   ## Step 6.7: Non-Testable Work Fallback (TDD-06)

   When verifying work that has no TDD-generated tests (non-testable tasks), adjust the verification approach:

   **Detection:** A task is non-testable if:
   - Plan frontmatter has `testable: false`
   - SUMMARY.md indicates the task was TDD-skipped (look for `[TDD-SKIP]` entries)
   - The task only produced documentation (.md), configuration (.json, .toml, .yaml), or planning artifacts

   **Fallback Verification Rules:**

   | Work Type | Primary Signal | Verification Approach |
   |-----------|---------------|----------------------|
   | Documentation (.md files) | T3 (goal-backward) | Verify docs exist, are substantive (>50 lines for meaningful docs), cover stated topics |
   | Configuration (.json, .toml, .yaml) | T3 (goal-backward) + mechanical | Verify config is valid JSON/TOML/YAML, contains expected keys, values are reasonable |
   | Research tasks | T3 (goal-backward) | Verify RESEARCH.md exists, covers research questions, has actionable findings |
   | Planning updates | T3 (goal-backward) | Verify plan files exist, have correct frontmatter, tasks are well-defined |
   | Mixed (testable + non-testable) | T1 for testable, T3 for non-testable | Verify each component with its appropriate signal |

   **For mixed plans:** When a plan contains both testable and non-testable tasks, split verification:
   - Testable tasks: Use T1 (test results) as primary
   - Non-testable tasks: Use T3 (goal-backward) as primary
   - Overall plan status: Combine both signals. If either fails, the plan has gaps.

   **Non-testable verification is NOT less rigorous.** It uses different signals, not weaker ones. Goal-backward analysis for documentation should be as thorough as test-based verification for code:
   - Does the documentation cover what the plan objective specified?
   - Is the configuration complete and valid?
   - Does the research answer the questions posed?

   **Report format for non-testable items:**

   In VERIFICATION.md, non-testable items should be reported in a separate sub-table:

   ```markdown
   ### Non-Testable Items (T3 Verification)

   | Task | Type | T3 Status | Evidence |
   |------|------|-----------|----------|
   | {task} | docs | VERIFIED | 120-line RESEARCH.md covers all 5 questions |
   | {task} | config | VERIFIED | Valid JSON, all expected keys present |
   ````

   ```

   ```

**Verification:**

- [ ] Step 6.7 exists in the verifier's `role` section content
- [ ] Fallback rules table covers docs, config, research, planning, and mixed work types
- [ ] Mixed plans split verification by testable/non-testable tasks
- [ ] Non-testable verification report format specified
- [ ] `bunx --bun tsc --noEmit` passes

---

### Task 4: Build and run full test suite

**Goal:** Confirm all modifications compile cleanly and all tests pass, including new and existing tests across all three plans in Phase 31.

**Steps:**

1. Run TypeScript compilation check:

   ```bash
   bunx --bun tsc --noEmit
   ```

   Confirm zero errors. All modified files (lu-verifier.agent.ts, lu-executor.agent.ts) must compile.

2. Run the TDD executor tests from Plan 31-02:

   ```bash
   bun test __tests__/src/agents/lu-executor-tdd.test.ts
   ```

   Confirm all tests still pass after the testable: false additions to the executor.

3. Run the lu-test-writer tests from Plan 31-01:

   ```bash
   bun test __tests__/src/agents/lu-test-writer.test.ts
   ```

   Confirm no regressions.

4. Run the full test suite:

   ```bash
   bun test
   ```

   Confirm no regressions across the entire suite. Pay special attention to:
   - Any existing verifier tests
   - Any existing executor tests
   - Agent compilation tests (all agents must still validate against agentConfigSchema)

5. Verify the lu-verifier agent still instantiates correctly:

   ```bash
   bun -e "const { LuVerifierAgent } = await import('./src/agents/general/lu-verifier.agent.ts'); const v = new LuVerifierAgent(); console.log(v.name, v.config.sections.length)"
   ```

   Confirm output shows `lu-verifier` with the expected section count.

**Verification:**

- [ ] `bunx --bun tsc --noEmit` passes with zero errors
- [ ] `bun test __tests__/src/agents/lu-executor-tdd.test.ts` passes (no regressions from Task 2)
- [ ] `bun test __tests__/src/agents/lu-test-writer.test.ts` passes (no regressions)
- [ ] `bun test` (full suite) passes with no regressions
- [ ] lu-verifier agent instantiates correctly with all sections

---

## Success Criteria

- [ ] lu-verifier Step 6.5 explicitly designated as T1 (PRIMARY) signal with rationale
- [ ] lu-verifier Step 9.5 explicitly designated as T3 (SECONDARY) signal with rationale
- [ ] Signal priority matrix in Step 9 defines how T1 and T3 combine into overall status
- [ ] T1 FAIL always results in `gaps_found` (mechanical failures are non-negotiable)
- [ ] T1 STRONG PASS with T3 PARTIAL results in `passed` (TDD tests confirm specification)
- [ ] lu-executor `tdd_execution_flow` has Step TDD-0 (testability check) with auto-detection
- [ ] `testable: false` in plan frontmatter skips TDD cycle in executor
- [ ] lu-verifier Step 6.7 documents fallback rules for non-testable work types
- [ ] Non-testable work uses T3 (goal-backward) as primary verification signal
- [ ] Mixed plans split verification by testable/non-testable appropriately
- [ ] `bunx --bun tsc --noEmit` passes
- [ ] `bun test` (full suite) passes with no regressions
