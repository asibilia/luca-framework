---
phase: 03-enterprise-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/luca-framework/src/commands/doctor.ts
  - packages/luca-framework/src/utils/redact.ts
  - packages/luca-framework/src/utils/doctor-checks.ts
  - packages/luca-framework/src/index.ts
  - packages/luca-framework/package.json
autonomous: true

must_haves:
  truths:
    - "User can run `npx luca doctor` and see diagnostic output"
    - "Node.js version check reports pass/fail with remediation"
    - "Cursor IDE detection reports status (pass/warn)"
    - "All secrets are redacted from diagnostic output"
  artifacts:
    - path: "packages/luca-framework/src/commands/doctor.ts"
      provides: "Doctor command entry point"
      min_lines: 80
      exports: ["doctorCommand"]
    - path: "packages/luca-framework/src/utils/redact.ts"
      provides: "Secret redaction utility"
      min_lines: 40
      exports: ["redactSecrets", "redactString"]
    - path: "packages/luca-framework/src/utils/doctor-checks.ts"
      provides: "Individual health check functions"
      min_lines: 100
      exports: ["checkNodeVersion", "checkCursorIDE", "CheckResult"]
  key_links:
    - from: "packages/luca-framework/src/commands/doctor.ts"
      to: "packages/luca-framework/src/utils/doctor-checks.ts"
      via: "imports check functions"
      pattern: "import.*doctor-checks"
    - from: "packages/luca-framework/src/commands/doctor.ts"
      to: "packages/luca-framework/src/utils/redact.ts"
      via: "imports redaction utility"
      pattern: "import.*redact"
    - from: "packages/luca-framework/src/index.ts"
      to: "packages/luca-framework/src/commands/doctor.ts"
      via: "registers doctor subcommand"
      pattern: "doctor.*doctorCommand"
---

<objective>
Create the foundation for the `npx luca doctor` diagnostic command with basic health checks and secret redaction capabilities.

Purpose: Enable users to diagnose Luca installation issues and environment problems. Secret redaction ensures diagnostic output can be safely shared in enterprise environments.

Output: Working doctor command with Node.js version check, Cursor IDE detection, and secret redaction utility.
</objective>

<execution_context>
@./.cursor/luca/workflows/execute-plan.md
@./.cursor/luca/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-enterprise-readiness/03-CONTEXT.md
@.planning/phases/03-enterprise-readiness/03-RESEARCH.md
@packages/luca-framework/src/commands/init.ts
@packages/luca-framework/src/utils/logger.ts
@packages/luca-framework/src/utils/detect.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fast-redact dependency and create redaction utility</name>
  <files>
    packages/luca-framework/package.json
    packages/luca-framework/src/utils/redact.ts
  </files>
  <action>
    Install fast-redact package: `bun add fast-redact` in packages/luca-framework/

    Create `packages/luca-framework/src/utils/redact.ts`:
    - Import fast-redact and create redactor instance with paths:
      - `env.*_API_KEY`, `env.*_TOKEN`, `env.*_SECRET`, `env.*_PASSWORD`
      - `mcp.*.env.*`, `config.mcp.*.env.*`
      - `*.password`, `*.secret`, `*.token`, `*.apiKey`, `*.api_key`
    - Export `redactSecrets<T>(obj: T): string` function that redacts and returns JSON string
    - Export `redactString(str: string, patterns: RegExp[]): string` for simple string redaction
    - Use `[REDACTED]` as censor value
    - Follow existing utils patterns (similar to logger.ts structure)

    Reference: Research shows fast-redact handles nested objects, wildcards, edge cases better than manual regex.
  </action>
  <verify>
    Run: `bun test` (if tests exist) or `bun run build` to verify compilation
    Check: `packages/luca-framework/package.json` includes `"fast-redact": "^3.0.0+"` in dependencies
    Check: `packages/luca-framework/src/utils/redact.ts` exports both functions
  </verify>
  <done>
    fast-redact installed, redact.ts created with both export functions, no TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create doctor check functions and CheckResult type</name>
  <files>
    packages/luca-framework/src/utils/doctor-checks.ts
    packages/luca-framework/src/types.ts
  </files>
  <action>
    Add `CheckResult` interface to `packages/luca-framework/src/types.ts`:
    ```typescript
    export interface CheckResult {
      name: string;
      status: 'pass' | 'fail' | 'warn';
      message: string;
      remediation?: string[];
      details?: string; // Shown in verbose mode
    }
    ```

    Create `packages/luca-framework/src/utils/doctor-checks.ts`:
    - Import CheckResult from types
    - Implement `checkNodeVersion(): Promise<CheckResult>`:
      - Check `process.version` major version >= 18
      - Return pass if >= 18, fail otherwise
      - Include remediation: upgrade instructions, nvm command, nodejs.org link
    - Implement `checkCursorIDE(): Promise<CheckResult>`:
      - Check for Cursor-specific paths: `~/.cursor`, `~/Library/Application Support/Cursor`
      - Use `existsSync` from fs (like detect.ts does)
      - Return pass if found, warn if not (best-effort detection)
      - Include remediation: install Cursor from cursor.com
      - Note: Detection is best-effort, may have false negatives

    Follow existing patterns from detect.ts for file system checks.
  </action>
  <verify>
    Run: `bun run build` to verify TypeScript compilation
    Check: `CheckResult` interface exported from types.ts
    Check: Both check functions return Promise<CheckResult> with correct structure
  </verify>
  <done>
    CheckResult type defined, both check functions implemented and return correct structure
  </done>
</task>

<task type="auto">
  <name>Task 3: Create doctor command with output formatting</name>
  <files>
    packages/luca-framework/src/commands/doctor.ts
    packages/luca-framework/src/index.ts
  </files>
  <action>
    Create `packages/luca-framework/src/commands/doctor.ts`:
    - Import `defineCommand` from citty
    - Import `* as p` from @clack/prompts
    - Import logger from utils/logger
    - Import checkNodeVersion, checkCursorIDE from utils/doctor-checks
    - Import redactSecrets from utils/redact
    - Import CheckResult from types

    Define doctorCommand with:
    - Meta: name 'doctor', description 'Check Luca installation and environment health'
    - Args: verbose (boolean, alias 'v', default false)
    - Run function:
      1. Call `p.intro('Running Luca Doctor...')`
      2. Run checks: `await Promise.all([checkNodeVersion(), checkCursorIDE()])`
      3. Display results:
         - Loop through results, use logger.success/error/warn based on status
         - Show icon: ✓ for pass, ✗ for fail, ⚠ for warn
         - Show name and message
         - If remediation exists, show as bullet list
         - If verbose and details exist, show redacted details
      4. Calculate summary: passed count, warnings count, failed count
      5. Show summary box using logger.box()
      6. Exit: 0 if no failures, 1 if any failures
      7. Call `p.outro('All checks passed!')` or `p.cancel('Some checks failed...')` appropriately

    Register command in `packages/luca-framework/src/index.ts`:
    - Import doctorCommand from commands/doctor
    - Add to subCommands: `doctor: doctorCommand`

    Follow existing command patterns from init.ts (citty structure, @clack/prompts usage).
  </action>
  <verify>
    Run: `bun run build` to verify compilation
    Run: `bun packages/luca-framework/bin/luca.js doctor` (or `npx luca doctor` if installed) to test
    Check: Command shows intro, runs checks, displays formatted output, exits with correct code
    Check: Verbose flag shows details when provided
  </verify>
  <done>
    Doctor command created, registered in index.ts, runs successfully with formatted output and correct exit codes
  </done>
</task>

</tasks>

<verification>
Run `npx luca doctor` and verify:
- Command executes without errors
- Node.js version check displays correctly (pass if >= 18, fail otherwise)
- Cursor IDE detection displays correctly (pass/warn based on detection)
- Output is formatted with icons and clear messages
- Remediation steps shown for failures/warnings
- Exit code is 0 when all pass, 1 when any fail
- Verbose mode shows additional details (when implemented)
</verification>

<success_criteria>
- `npx luca doctor` command exists and executes
- Node.js version check works correctly
- Cursor IDE detection works (best-effort)
- Secret redaction utility available for future use
- Output is enterprise-safe (no secrets exposed)
- Exit codes correct for scripting use
</success_criteria>

<output>
After completion, create `.planning/phases/03-enterprise-readiness/03-01-SUMMARY.md`
</output>
