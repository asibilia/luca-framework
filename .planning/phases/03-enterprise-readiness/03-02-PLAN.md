---
phase: 03-enterprise-readiness
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - packages/luca-framework/src/utils/doctor-checks.ts
  - packages/luca-framework/src/utils/mcp-validator.ts
  - packages/luca-framework/src/commands/doctor.ts
autonomous: true

must_haves:
  truths:
    - "MCP configuration validation reports structure issues"
    - "Framework version check compares installed vs latest"
    - "Config file validation checks .planning/config.json structure"
    - "Network connectivity check tests GitHub API (optional)"
  artifacts:
    - path: "packages/luca-framework/src/utils/mcp-validator.ts"
      provides: "MCP configuration validation"
      min_lines: 80
      exports: ["validateMCPConfig", "checkMCPConfig"]
    - path: "packages/luca-framework/src/utils/doctor-checks.ts"
      provides: "Additional health check functions"
      min_lines: 150
      exports: ["checkMCPConfig", "checkFrameworkVersion", "checkConfigValidation", "checkNetworkConnectivity"]
  key_links:
    - from: "packages/luca-framework/src/utils/mcp-validator.ts"
      to: ".cursor/mcp.json"
      via: "reads and validates JSON structure"
      pattern: "readFile.*mcp\\.json"
    - from: "packages/luca-framework/src/utils/doctor-checks.ts"
      to: "packages/luca-framework/src/utils/manifest.ts"
      via: "reads manifest for version check"
      pattern: "readManifest"
    - from: "packages/luca-framework/src/utils/doctor-checks.ts"
      to: ".planning/config.json"
      via: "validates config structure"
      pattern: "readFile.*config\\.json"
    - from: "packages/luca-framework/src/commands/doctor.ts"
      to: "packages/luca-framework/src/utils/doctor-checks.ts"
      via: "calls new check functions"
      pattern: "checkMCPConfig|checkFrameworkVersion|checkConfigValidation"
---

<objective>
Extend doctor command with advanced health checks: MCP configuration validation, framework version comparison, config file validation, and optional network connectivity testing.

Purpose: Provide comprehensive diagnostics for enterprise environments. MCP validation helps troubleshoot integration issues. Version checking ensures users know when updates are available. Config validation catches setup errors early.

Output: Doctor command with 4 additional checks beyond basic Node/Cursor checks.
</objective>

<execution_context>
@./.cursor/luca/workflows/execute-plan.md
@./.cursor/luca/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-enterprise-readiness/03-CONTEXT.md
@.planning/phases/03-enterprise-readiness/03-RESEARCH.md
@packages/luca-framework/src/utils/manifest.ts
@packages/luca-framework/src/utils/wizard.ts
@packages/luca-framework/src/utils/redact.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MCP configuration validator</name>
  <files>
    packages/luca-framework/src/utils/mcp-validator.ts
  </files>
  <action>
    Create `packages/luca-framework/src/utils/mcp-validator.ts`:
    - Import readFile from fs/promises, existsSync from fs, join from pathe
    - Import CheckResult from types

    Implement `checkMCPConfig(cwd: string): Promise<CheckResult>`:
    - Check if `.cursor/mcp.json` exists (use join(cwd, '.cursor', 'mcp.json'))
    - If not found: return warn status with message ".cursor/mcp.json not found"
      - Remediation: "MCP servers are optional but recommended", "Configure in Cursor Settings → MCP"
    - If found: try to parse JSON
      - Validate structure: check if `mcpServers` is an array
      - If invalid structure: return fail with remediation about array requirement
      - If valid: iterate through servers
        - Check if server.name exists (warn if missing)
        - Check if server.command exists and executable exists (use existsSync on first part of command.split(' ')[0])
        - Collect issues array
      - Return pass if no issues, warn if issues found
    - Catch JSON parse errors: return fail with remediation about JSON syntax

    Follow pattern from RESEARCH.md Pattern 3. Do NOT test actual connectivity (MCP servers run in separate processes).
  </action>
  <verify>
    Run: `bun run build` to verify compilation
    Test: Create test .cursor/mcp.json files (valid, invalid structure, missing file) and verify correct results
    Check: Function returns CheckResult with appropriate status and remediation
  </verify>
  <done>
    MCP validator created, handles missing file, invalid JSON, invalid structure, and missing server fields correctly
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement framework version and config validation checks</name>
  <files>
    packages/luca-framework/src/utils/doctor-checks.ts
    packages/luca-framework/src/utils/version-check.ts
  </files>
  <action>
    Add to `packages/luca-framework/src/utils/doctor-checks.ts`:

    Import readManifest from utils/manifest, readFile from fs/promises, join from pathe, semver from semver
    Import CheckResult from types

    Implement `checkFrameworkVersion(cwd: string): Promise<CheckResult>`:
    - Read manifest from `.planning/manifest.json` using readManifest(cwd)
    - If no manifest: return warn "No manifest found (Luca not initialized?)"
    - If manifest exists:
      - Get installed version from manifest.version
      - Get current package version (read package.json from luca-framework, use version-check.ts pattern)
      - Compare using semver: if installed < current, return warn "Update available"
      - If installed === current, return pass "Up to date"
      - Include remediation: "Run `npx luca update` to update" if outdated
      - Include details: installed version vs current version

    Implement `checkConfigValidation(cwd: string): Promise<CheckResult>`:
    - Check if `.planning/config.json` exists
    - If not found: return warn "Config file not found"
    - If found: try to parse JSON
      - Validate required fields exist (check for common fields like mode, depth if applicable)
      - If invalid: return fail with remediation about config structure
      - If valid: return pass "Config file valid"
    - Catch JSON parse errors: return fail with remediation

    Reference existing config loading patterns from wizard.ts if available.
  </action>
  <verify>
    Run: `bun run build` to verify compilation
    Test: With manifest present/absent, config present/absent, verify correct status reporting
    Check: Version comparison uses semver correctly
  </verify>
  <done>
    Framework version check compares installed vs current using semver, config validation checks file existence and JSON validity
  </done>
</task>

<task type="auto">
  <name>Task 3: Add network connectivity check and integrate all checks into doctor command</name>
  <files>
    packages/luca-framework/src/utils/doctor-checks.ts
    packages/luca-framework/src/commands/doctor.ts
  </files>
  <action>
    Add to `packages/luca-framework/src/utils/doctor-checks.ts`:

    Implement `checkNetworkConnectivity(): Promise<CheckResult>`:
    - Use fetch to test GitHub API: `https://api.github.com` with timeout (5 seconds)
    - If succeeds: return pass "Network connectivity OK"
    - If fails/timeout: return warn "Network connectivity issues"
      - Remediation: "Check internet connection", "Firewall may be blocking GitHub API"
      - Details: include error message (redacted if contains secrets)
    - Make this optional/graceful: don't fail doctor if network check fails (it's a warning)

    Update `packages/luca-framework/src/commands/doctor.ts`:
    - Import new check functions: checkMCPConfig, checkFrameworkVersion, checkConfigValidation, checkNetworkConnectivity
    - Add checks to Promise.all array:
      - checkMCPConfig(process.cwd())
      - checkFrameworkVersion(process.cwd())
      - checkConfigValidation(process.cwd())
      - checkNetworkConnectivity() (optional, can be last)
    - Ensure all checks run and results display correctly

    Use redactSecrets for any error details that might contain sensitive info.
  </action>
  <verify>
    Run: `bun run build` to verify compilation
    Run: `npx luca doctor` and verify all checks execute
    Check: Network check handles offline gracefully (warn, not fail)
    Check: All checks display in output with correct status icons
  </verify>
  <done>
    Network connectivity check implemented, all checks integrated into doctor command, output shows all 6 checks (Node, Cursor, MCP, Version, Config, Network)
  </done>
</task>

</tasks>

<verification>
Run `npx luca doctor` and verify:
- MCP config check reports structure issues correctly
- Framework version check compares versions and suggests updates
- Config validation checks .planning/config.json
- Network connectivity check tests GitHub API (warns if offline, doesn't fail)
- All checks display in formatted output
- Exit code reflects overall health (0 if all pass, 1 if any fail)
</verification>

<success_criteria>
- MCP configuration validation works (structure check, no connectivity test)
- Framework version comparison uses semver correctly
- Config file validation checks JSON structure
- Network check is optional and graceful (warn, not fail)
- All checks integrated into doctor command output
- Doctor command provides comprehensive diagnostics
</success_criteria>

<output>
After completion, create `.planning/phases/03-enterprise-readiness/03-02-SUMMARY.md`
</output>
